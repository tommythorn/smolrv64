# Plan: IPC, in measured order (2026-09-05)

Every item below is justified by a number measured on this core this week, names the
mechanism, the effort, and the gate. Each non-trivial RTL change is validated on the board
to `login:` with zero faults (`tools/board-gate.sh`) before the next lands on top of it, and
work continues while a bitstream builds and boots. Status is kept in this file.

## The measurements this plan rests on

**sha256sum on a cached 30 MB file, board, bitstream I (main 4a769238), `perf stat` with the
full event set** (the first run had stopped at r0312 and reported 46% "unattributed"):

    cycles 1,864,874,990   instructions 811,970,497   IPC 0.435   CPI 2.297
      issue / retire                     1.002   43.6%
      stall: LSU                         0.168    7.3%   (1.14 cycles per D$ access, 0.7% misses)
      stall: serializing op              0.123    5.4%   (syscalls: 30 MB in 4 KiB reads)
      frontend bubble                    0.997   43.4%
        iMMU walking                     0.005    0.2%
        no fetch bytes at all            0.032    1.4%   <- the I$ is NOT the bubble
        bytes, but no whole instruction  0.250   10.9%   <- a 32-bit op straddling the 8-byte window
        insn ready, F/X queue empty      0.708   30.8%   <- the queue's own cycle, paid whenever fetch falls behind
      redirects 7.8 /k insn: 3.0 branch, 2.9 jalr, 1.6 other; 70% land in the fetch buffer

**Boot (tiny128, 60 M cycles, the DDR model measured at 166.67 MHz), `SB-WHERE2`:**
dispatch held on a FULL STORE QUEUE >= 16 M cycles at either latency; load queue full 0.5 M;
M empty with the F/X queue dry 22.6 M at the board's latency; D$ fill busy 33%, I$ 15%.

**Microbenchmarks (L1-resident):** load-use 5.00 cycles (early start), load throughput
2.25; a store that hits 5.88 cycles; a store then a load of the same word 11.75.

**DDR (board, 166.67 MHz):** reads mean 28.75 cycles (96.5% in 16-31), writes 15.6.

**fence.i (simmerv census):** 4-15 k per boot, each two full cache walks; none in Geekbench.

**The multiple-outstanding-loads branch:** +1.3% at the measured DDR latency, +4.4% at a
flat 4 cycles; its wall at the board's latency is the store path.

## The items, ranked by payoff per effort

### 1. Name the bubble -- DONE 2026-09-05
The perf run above. `tools/perf-cpi-stack.py`'s usage line now lists r0313-r0316, so the
straddle and queue buckets are always collected. Later the same day: the tool's "residual"
was zero by construction (cycles minus stalls, plus stalls), so it now reports UNATTRIBUTED
against the one-per-cycle floor, `ST_ROB` is a stack term, and `tools/perf-smol.sh cpi` is
the whole 13-counter file (ST_*, FE_*, RD_WAIT, REDIR, DCMISS). A run with only the FE_*
events is not a stack: every backend stall lands in "retire", and the tool now says so.

### 2. Fetch: a 16-byte window that keeps the F/X queue fed
**Why:** 41.7% of sha256's cycles are the two fetch buckets above. The window is `HW=4` =
8 bytes, two 32-bit instructions; the fetch buffer keeps ONE I$ request in flight; a 32-bit
instruction across the window boundary is refetched (the aligner "excludes a straddling
32-bit op from the window", `src/fetch.v`), and every cycle the aligner produces nothing,
the F/X queue runs empty and the next instruction pays the queue's registration cycle.
**What:** `OOO2_HW=8`: 16 bytes per window, four 32-bit instructions, straddles halved, and
the queue stays non-empty at one dispatch per cycle. It is gated only by
`smolrv64_sdpram`'s geometry guard on the I$ read width (`RDW=128`). Then a second I$
request in flight from the fetch buffer (the I$ accepts one read per two cycles, so two in
flight is the same 8 bytes/cycle the window gives). A leftover-halfword buffer in the
aligner (merge the straddling half with the next window instead of refetching) is the
third step if the straddle bucket is still visible after the window.
**Measure:** a bare-metal straight-line 32-bit ALU loop (`workloads/febench`) under the
testbench, before and after; then sha256sum's stack on the board.
**Effort:** days. **Timing risk:** the aligner's mux widens; the I$ has 40 MHz OOC margin.
**Gate:** febench, the cosim, the board.

### 3. Senior store queue (the irrevocability pointer) -- DONE 2026-09-04 (board pending)
**Why:** a store's ROB slot completes when the cache takes the write, so the head sits on
every store for ~6 cycles and the 16-entry ROB starves dispatch on store-dense code.
**What:** a second ROB pointer walks forward from the head over entries that are done and
cannot restart (not faulted, not a mispredicted branch awaiting its drain, not a system,
fence.i or interrupt pseudo-op; in-flight loads do not block it, their fault is decided
before the access). Stores at or behind it are committed: their ROB slot completes, the
head moves on, the store queue drains behind retirement. The store queue's flush becomes
partial (keep committed entries, drop from the first uncommitted); "uncommitted" for the
load alias check keeps its meaning (still in the queue). Side effect: more loads find no
older store live and take the early start, a cycle each.
**Effort:** a day or two. **Gate:** tb_ooo2_lqsq (+ a case for the partial flush), the
random bench, cosim, the board.

### 4. The cache's write path -- DONE 2026-09-05 (4a+4b on the board as N; 4c since P4/Q, the bisection cleared it)
**Why:** the store queue is full for >= 16 M of 60 M boot cycles because the D$ takes one
write per ~6 cycles, and only while no fill is in flight (a write is SOLO). Item 3 removes
the coupling to the head; this removes the six cycles.
**What:** ack a store on ACCEPT, take a write under a fill, pipeline the write behind the
next lookup (a write is solo because it shares the fill machine's window registers). The
hazard, a load to a line whose write is still in the pipeline, is a one-entry compare.
**Measure:** stbench 5.88 -> ~2 cycles per store; the boot's `sq_full`.
**Effort:** days, in the timing-critical module (rules I6-I9; the census on the shipping
directive judges it). **Gate:** run-ooo2-cache-tb both shapes, stbench, cosim, the board.

### 5. The mispredict drain
**Why:** a resolved branch waits for the ROB head before it redirects (`head_block`), up
to 16 retirements plus the refetch. sha256 has 7.8 redirects per thousand instructions;
the branchy Geekbench workloads (Navigation, HTML5, SQLite) have several times that.
**First:** one counter, cycles from resolution to redirect, on the stack. Then P7 (the
rename walk-back) if it is large.
**Effort:** the counter an hour; P7 days. **Gate:** cosim, the board.

### 6. D$ next-line prefetch, as an experiment -- BUILT, OFF: faults on the board (2026-09-05)
**Why:** the D$ fill machine is busy 33% of boot cycles at one line per ~35 cycles, and
page copies and memsets are sequential. The I$ already has the stream buffer (`PF_EN`).
**What:** `PREFETCH(1)` on the D$ instance plus one rule: any CBO or invalidate drops the
buffer (DMA memory is reconciled by CBO only; a stale prefetched line must not survive it).
**Measure:** boot retires at 60 M cycles; a memcpy microbench.
**Effort:** an afternoon. **Gate:** cache tb (add a prefetch-vs-CBO case), cosim, the board.

### 7. virtio-net burst DMA -- word DMA DONE 2026-09-05 (board pending); bursts later
**Why:** NFS boots and every file over the network are bound by the device, and its queues
are already 256 deep. `src/virtio_net.v` fetches each 64-bit word of a frame with its own
DMA round trip (`S_FRAME_RD -> S_WAIT_FRAME`) and copies it into the engine one byte per
clock (`S_FILL`); a 1500-byte frame is ~190 round trips plus 1500 byte-cycles, on the order
of ten thousand cycles per packet each way.
**What:** read and write frames as 64-byte lines (the line port, one request per 8 words)
or as pipelined word requests with several outstanding; move bytes into and out of the
engine 64 bits at a time. Both queues, TX and RX.
**Measure:** `iperf3` / a large `dd` over NFS on the board, before and after.
**Effort:** days. **Gate:** src/run-virtio.sh (net variant), the board with NFS root.
**Note:** virtio-blk's queue is 8 (two requests in flight). Raising it to 128 in the board
top and the bench panicked at the root mount on 2026-09-05 (`VFS: Unable to mount root fs
on unknown-block(254,1)`); a queue-8 control run is in flight. Not committed until the
device works at the deeper queue; the indices are 16-bit throughout, so the cause is
elsewhere (feature negotiation or the SD backend's chain walker are the suspects).

### 8. An L2 in URAM
**Why:** the KU5P has 2.3 MB of URAM unused; DDR reads cost 29 cycles; the board's operating
point is miss-bound (D$ fill busy a third of boot cycles). A 1 MB L2 at a handful of cycles
is a several-fold cut in miss cost for every working set that fits.
**What:** a direct-mapped or 2-way URAM cache on the 64-byte line port, physically
addressed, write-back, with the DDR behind it; the testbench's DDR model gains the same
stage so the cosim measures it.
**Effort:** a week. **Gate:** cache tb for the new module, cosim at the measured model, the
board.

### 9. Load-use: VHPR plus squash-and-replay
**Why:** the bank address is the PA, which is late (the dTLB compare at the end of M), so
the cache accepts in M+1; a virtual index and tag make it the early VA and the accept cycle
disappears: 5.00 -> 4.00 cycles per load; a raw BRAM word delivered in the compare cycle
would make it 3, if BRAM -> way select -> PRF write fits 6 ns (OOC to prove). ~3% of boot
cycles; more on pointer chasing.
**What:** the read starts on the VA in M; the alias verdict (`e_block` for the entry the tag
names, known at the end of M) and the translation fault cancel the response by tag and the
entry becomes a queued candidate (the existing path); a TLB miss drops and requeues.
VHPR's synonym, ASID, epoch and CBO machinery per docs/VHPR.md; CBO on the I$ through the
physical tags. **Before it:** DMA and synonym coverage in simulation (the D$ bench's T8-T13
and a system-level DMA agent), because both bugs of 2026-09-04 lived exactly there and the
cosim is blind to it.
**Effort:** weeks. **Gate:** the coherence benches, cosim, the board.

### 10. Two-wide dispatch -- IN PROGRESS (10a in sim 2026-09-05)
**Why:** the one-per-cycle floor is 87% of sha256sum's cycles on Q; nothing else left is
that large. Staged so each step is board-gated with the stack measured:
- **10a, fetch two per cycle** (branch wip/iw2): `IW=2` into a banked F/X queue, decode pops
  one. Bundles are cut deterministically (first CTI/SYSTEM, or the 16-byte chunk boundary
  after slot 0); the predictor is keyed by the bundle base at training too (the slot's
  offset rides in the details); the ahead-PC length table is 4,096 entries. Pays little by
  itself (every fetch gap absorbed); it is the frontend 10b needs. Board: build T after S.
- **10b, dispatch two per cycle.** Two decodes and IRs, read straight from the queue's two
  banks. Rename: the speculative map becomes two LUTRAM copies plus a per-entry "newer" bit
  (one write port each; the same lv[] trick that already picks speculative over committed),
  six read ports by duplication, the intra-pair bypass (B's source = A's destination takes
  A's new register), two free-list reads per shard (h += 2). ROB: two allocations into
  two parity banks of entries, one write each (tail += 2). Schedulers and payload memories keep ONE write port each: a pair dispatches
  only to two different schedulers (ALU+mem, ALU+FP, mem+FP); otherwise B waits a cycle.
  LQ/SQ one each per cycle. Gate: riscv-tests, the random benches, cosim, the board; the
  measure is the GB5 cosim (mixed code) more than sha256 (80% ALU: it needs 10d).
- **10c, retire two per cycle.** The commit port doubled: RMAP and the free lists take two
  writes (the two-copy trick again), the store commit walk stays one per cycle, a second
  retire only for a plain done entry (never a trap, system op or redirect), the cosim probe
  emits two records. Without it the 16-entry ROB fills at dispatch rate and 10b pays nothing.
- **10d, a second ALU.** The physical register number carries the shard in its top two bits
  and code 3 is free: a fourth shard (SH_IE2, 64 entries, written by ALU2 alone), a second
  integer scheduler picking into ALU2 with its own operand read ports, a fifth ROB completion
  port, one more wakeup port on every scheduler (NWB 3 -> 4), and a second forward from
  ALU2's writeback register on each operand. Slot A's ALU ops rename into SH_IE and go to
  scheduler 0, slot B's into SH_IE2 and scheduler 1: a pair of ALU ops then never wants one
  write port, by construction, and the 10b rule keeps its one site. Crude balance (a lone
  ALU op is always slot A's) is the first cut; measure before alternating. This is what
  turns sha256-shaped code from 1.0 toward 2.0 at the floor; its timing risk is the
  operand path, which is why the ALU writeback was registered first (below).
- **Before 10d: the ALU writeback is registered** (branch wip/alu-wb, 2026-09-05). Read ->
  ALU -> PRF write in one cycle was the critical path (13 levels, 82% route, +0.001 ns on Q,
  -0.034 on S); the value now lands in `alu_q` and is written a cycle later, with a one-deep
  forward covering the single cycle a dependent could read early; everything issue-timed
  (wake, pending clear, the store queue's data snoop, the ROB's done, the cosim capture)
  stays at issue. riscv-tests 240/240, lint clean; the cosim and the board (build U) say
  whether it costs cycles (it should not) and what it buys in slack.
**Effort:** weeks. **Ceiling:** 166.67 MHz is not tradeable (the next rung is 111).

### V. A vector unit without giving up the clock -- ASSESSED 2026-09-07, not designed

Tommy's question after the release: is adding V possible at all without sacrificing
166.67 MHz? Facts first. Geekbench 7's RISC-V preview on the board ships THREE binaries,
`geekbench_rv64gc`, `geekbench_rv64gcbv` and `geekbench_rva23` -- so GB7 runs on this core
today with the scalar one, and the open question about it is the 2 GiB, not the ISA; GB6
ships `geekbench_riscv64` and `geekbench_rv64gcv`. Simmerv implements RVV 1.0 (Zve64d and
the full set, Zvfh) at VLEN=128 under `--rva23`, so the instruction mix of the `rv64gcbv`
build -- how many of the three SIMD subtests' instructions become vector ops, at what vl --
can be measured there before any RTL exists, and that is the first step.

The shape that keeps the clock, if it is built: a decoupled in-order vector island behind
stage M. Vector ops are M-class (they issue in program order from `u_iq_l`, behind every
older branch, so when one leaves M into the island's FIFO nothing older can restart it and
the FIFO never needs a squash); each op carries its own vl/vtype snapshot taken at M, so
`vsetvli` is a one-cycle M op writing rd through the LD shard and vl/vtype in csr_file,
not a serializing CSR; the island executes in order with chaining, on 2 x 64-bit integer
lanes (DSP multipliers) and two fpnew lanes at the same PIPE_REGS=4 that closes today; a
32 x VLEN=128 register file is 4 Kbit of LUTRAM or one BRAM per port; vector FP flags OR
into fcsr like the FPU's; scalar results (vmv.x.s, vcpop, vfirst, vsetvl) come back through
M's LD-shard write, so the wakeup broadcast, the schedulers and the PRF shards do not
change. Vector loads and stores go through the existing LSU FSM element by element (one
64-bit chunk per cycle unit-stride, one element per access otherwise), which makes the
precise vstart trap semantics free and adds no port to the D$ door.

The two real risks, both measurable before the unit is written: (1) rule I1 -- 35-45 k
LUTs of island (two fpnew lanes are most of it) in a design that is route-bound and
placement-sensitive (five BRAM tiles cost 434 ps once), mitigated by a pblock in the
clock regions the core does not use (`PBLOCK_CORE` exists; the part is 38% full); (2) the
LSU FSM's growth, whose enables (`pt_v`, `ld_land`) were the W6 fan-out leaders. The
experiment that answers both: a dummy island of the target size, registered at its
boundary, pblocked away from the core, in a full build -- if `probe_clk` holds within the
placer's spread, the clock survives the area; then the OOC of one lane pair at 6 ns. Payoff
bound: Machine Learning at ~6 G scalar instructions per image with a VLEN=128, two-lane
unit is a 3-5x kernel speed-up at best, a score of 1-2 not 10; the reason to do it is GB7's
`rv64gcbv`/`rva23` builds and RVA23 itself, not the three scores.

## Not IPC, but decides whether any of it ships

### T1. Timing margin for the two-wide core -- the two structural families left
**Why:** the two-wide core (10b-10e) sits about a nanosecond inside the placer's variance
at 166.67 MHz (V4 0.000, V5 -0.968 on a smaller design); what the reports name, once M's
completion is out of every enable (gate V3's rule I11, V6), is two route-dominated cones.
- **(F) The fetch cycle.** PC (or the interrupt inject) -> iTLB compare -> the fetch
  buffer's window validity -> the aligner -> the F/X queue's data pins, 17-20 levels, 75-80%
  route. The queue is 8 deep and absorbs a cycle of fetch latency for free in straight-line
  code; only a redirect pays it. Cut the cycle at the fetch buffer's output: register the
  served window and its `avail` (the aligner and the queue write run a cycle later), and
  register the redirect target into the frontend (one cycle per mispredict, ~0.3% at 2-3
  per thousand). The ahead-PC machinery (`apc`, `lenp`) already keeps the BTB read off this
  path; it moves with the window.
- **(W) The M port's PRF read.** The issue register's tag drives every read-port replica of
  every shard (fan-out 387, 1.1 ns before the LUTRAM), then address generation or the
  branch compare, into `m_result` -- 20 levels. Two levers: fewer replicas (the ALU ports
  never need the FE shard if an ALU op with an FE-mapped source, i.e. a value written by
  `fcvt.w`/`fmv.x`/`fle`, is routed to the M/F port at dispatch -- the shard is in the
  renamed tag), and a register between the read and M's execute only if (F) and the
  replica cut are not enough, because that one costs load-to-use and branch latency.
- **(L) The landing load's yield into the issue enable** (gate V6's worst family, -0.319:
  `u_lq/sqt -> ps_out0/CE`, 23 levels). The LD shard has two writers, a landing load and M's
  mul/div/CSR/jump result, so M yields to a landing (`m_done & ~ld_land`), and the landing is
  the load queue's store-ordering decision in the landing cycle. Through `m_advance` that
  decision is the M/F issue register's clock enable and the in-order scheduler's take. The
  fix the spec already names: one writer per shard -- landing loads on their own shard, or
  M's non-load results elsewhere -- which needs a third shard bit (tags 9 -> 10 everywhere)
  now that codes 0-3 are taken; the cheap half is to yield only when M's op writes LD (a
  store or branch never does), which buys cycles, not slack.
**Measure:** WNS and the worst family per gate; the sha256 kernel and the boot cosim for the
cycle each cut costs. **Effort:** (F) two days, (L) two days, the replica cut a day.
**Gate:** the usual. Gates V4-V6 (0.000 / -0.968 / -0.319 on 4,172 endpoints) are the
baseline: the placer's roll is a nanosecond wide on this core, so the margin has to be
structural.

### F1. A flight recorder for wedges
A retire watchdog that, after N cycles without a retirement, freezes a register block (PC,
ROB head state, LSU FSM state, `o_v`, queue occupancies, the last DDR request) readable
over the JTAG debug hub with no ILA build per probe set. The J wedge (2026-09-05,
Geekbench's Structure from Motion, no console output, no ping) could not be asked anything.
Until it exists: J rebuilt with `ILA_CORE=1` plus a probe set on the fast path.

### F2. A coherent I$ with fence.i as a drain
Write-update on resident lines, CBO honoured by both caches through the physical tags, the
I$ miss cleaning its line in the D$ before filling, fence.i as pipeline drain plus queue
drain. Removes two full cache walks per fence.i: 4-15 k per boot, none in benchmarks. Boots
and shells only; after item 9, whose physical-tag path it uses.

## Order for the next batches

Batch A (now): item 2's bench and window; item 3 in parallel; the queue-8 virtio control.
Batch B: item 4 behind item 3; item 5's counter; item 6 as the cheap experiment.
Batch C: item 7 (virtio-net), item 8 (L2).
Then 9, then 10.

## Status log

- 2026-09-07 15:30, THE RELEASE (Tommy: "clear out anything that isn't relevant for OOO2,
  delete or update any document that is out of date, rewrite the README"). Branch
  wip/release, gated like any RTL change because the FPGA top changed. Removed: the sharded
  OoO core and its harness (src/backend_top.v, soc_top.v, exec_shard.v, the shards, cache.v,
  lsu.v, 32 benches, run-vl-tests.sh, sweep.sh, the probe flow, the sequential core's
  remnants), the perf event trace and perftool, the ulx3s platform (Tommy: it will never run
  this core), the 5.4-kernel and ubuntu-mini workloads, the ubuntu DTS variants nothing
  booted, the .even/.odd test images, 30 docs from before this core, and the dead iverilog
  riscv-tests runner (it could not elaborate the frontend). rk_xcku5p.v keeps only
  rv_soc_top (2234 -> 1973 lines: the scalar arm, the soc_top arm, PROBE_CORE/OOO2_CORE,
  NO_VIRTIO_WIRE, ILA_TIMER and the core_clk/fpu_clk buffers are gone); build.tcl and the
  Makefile lose the same knobs (681 -> 551 lines). Dated OOO2 records moved to
  docs/history/. src/run-tb.sh now finds each bench's top module and accepts each bench's
  own PASS line: 19/19, where the same benches read 23/45 before (seven of the kept ones
  had been counted as failures by the runner for months). OOO2-Spec.md, rtl-rules.md,
  CLAUDE.md and the README rewritten for the one-core tree; the README carries the
  Geekbench 5 chart (tools/gb5-chart.py from workloads/gb5/result-2026-09-07.md), colour
  coded by category as Tommy asked, to be extended with GB6 and SPEC. Queued after this
  (Tommy): is a vector unit possible at all without giving up 166.67 MHz? Simmerv runs
  Geekbench 7, so the instruction-mix questions can be answered there first.
  GATED 2026-09-07 16:25 (384fd599, after a first build failed at the netlist boot because
  the tracked .xpr still listed the 27 deleted sources -- cleaned, and build.tcl now drops
  missing files itself): lint clean; 240/240; every bench; src/run-tb.sh 19/19; the Linux
  cosim 300 M cycles clean at 78,964,298 retires (recorded in cosim-expected.txt); netlist
  boot PASS; `probe_clk` WNS +0.023 ns on the default directive, 83,093 LUTs (38.3%), 122
  BRAM tiles, 28 DSPs; the board: `login:` with zero faults, banner rtl=384fd599
  (/var/tmp/ooo2_REL_release_166MHz_384fd599.bit, gate-results/384fd599). Main fast-forwarded
  to it; tag board/REL-onecore. The same board session first ran the counter split's turn
  (3bb7947f's bitstream, banner 9d027fee): `login:` with zero faults, so its "board pending"
  above is resolved. Geekbench 6.7.1 single-core (Tommy's install, under the `cpi` event
  set) launched on the release bitstream right after.
- 2026-09-05: item 1 done. Plan written.
- 2026-09-05: item 2, step 1 (the 16-byte window) in simulation. `workloads/febench`
  (straight-line 32-bit ALU code, 258 k instructions per block; the fetch rate is the IPC):

      block                        HW=4    HW=8
      32-bit, 4-byte aligned        0.66    0.98
      32-bit, 2-byte offset         0.40    0.79    (the straddle cost: the leftover-halfword item)
      compressed                    0.99    0.99

  The I$ keeps 64-bit banks: `rv_cache` caps BANKW at 64 and a 128-bit read is the even/odd
  chunk pair at a 16-byte-aligned address (asserted), so the width-cascade BRAM geometry that
  fetched garbage in the old wide I$ is never built. riscv-tests 240/240 at HW=8; the tiny128
  cosim +0.4% (the HW=8 row of cosim-expected); board K (2026-09-04): login, 0 faults, WNS +0.071.
- 2026-09-04: item 3 in simulation. stbench (cycles per op, main before -> after): 8
  independent stores 5.88 -> 5.00; 4 stores + 4 loads 4.88 -> 4.50; store then load of the
  same word 11.75 -> 11.75. tiny128 boot +0.22% retires at 60 M cycles. The ROB's
  irrevocable pointer stops at any not-done entry (loads in flight included: passing them
  needs a write-after-read check in the load queue, deferred until item 4 makes it worth
  measuring). Two ordering gates that used the ROB as a proxy for "older stores visible"
  now name the store queue (rule C5): the serialization drain, and the CBO start in M --
  the latter was a pre-existing gap for a store not yet at the head. Board: build L.
- 2026-09-04: build L (25813a27) hung at SLUB init, WNS +0.037. The CBO gate waited for
  an EMPTY queue, and entries are allocated at dispatch: the store after a cbo.zero sits in
  the queue without an address until M frees, which M never does. Fixed to wait for an
  entry WITH an address (only an older store has one). `workloads/fphammer/cbozero.c`
  reproduces it in 4 M cycles (129 retires, then nothing); the tiny128 boot issues no
  cbo.zero, so the Geekbench-image cosim to SLUB init (~400 M cycles) is the CBO gate.
  Board: build M (senior queue, fixed, at OOO2_HW=8 -- K validated the window).
- 2026-09-05: item 4a (the store leaves the LSU at the D$'s accept; CBO and uncached writes
  wait for a completion the D$ raises only for them) and 4b (the D$'s door is open in a plain
  write's last cycle, minus the set being written). stbench per store: 5.00 -> 3.00 (4a) ->
  2.31 at HW=8 (4b); at HW=4 4b shows nothing because the loop is fetch-bound (M empty 63%).
  tiny128 boot +14.5% (4a) +1.3% (4b) at 60 M cycles. Two defects found on the way by the
  cosim and the new cbozero bench, neither visible to riscv-tests: the plain write's ack
  completing a later device store, and a device store (no NC bit without page tables)
  waiting on a cache accept while the device path re-executed it. `SB-STORE` counters added
  to tb_ooo2_linux. 4c (a write miss completed by the fill machine, writes under fills) next.
- 2026-09-05: item 4c: the write miss is merged by the fill machine as the line lands and
  installs dirty, so a plain write never replays and is accepted under a fill like a read
  (F_ANS gets its own window; a write's S_FIN waits out an install; a write that hits the
  fill's victim re-looks). tiny128 +2.35% at 60 M cycles. tb_ooo2_dcache T15-T17; the bench's
  accept handshake was wrong (it dropped the request before the edge that latches it and then
  saw a phantom accept) -- store_go now holds through the posedge. Board: build O.
- 2026-09-05: item 7, first step. `src/tb_virtio_net.v` (new: the backend against an AXI
  memory model and stub engines, every byte and the used rings checked) measured the shipping
  device at RX 31,919 and TX 7,839 cycles per 1500-byte frame -- RX wrote every byte with its
  own AXI transaction. Now RX gathers 8-byte words (one write each, the previous in flight)
  and TX reads the next word ahead of the byte fill: RX 3,953, TX 6,534. On the board with
  bitstream M a cold 100 MB read over NFS took 193 s (0.5 MB/s). Board: build P.
- 2026-09-05: item 2 shipped: `OOO2_HW=8` is the default in the RTL, build.tcl and the sim
  runners (builds K, M and N validated it on the board). sha256sum's CPI stack on M (senior
  queue + HW=8): IPC 0.714, from 0.435 on I -- frontend bubble 43.4% -> 10.1% of cycles
  (F/X queue empty 7.8%, straddle 0.5%), LSU 11.3%, serializing 6.9%, issue/retire 71.4%.
  The leftover-halfword aligner (item 2 step 3) is not worth its cycle at 0.5%.
- 2026-09-05: item 6: the D$ gets the I$'s next-line stream buffer, with the rules a
  writable cache needs (a writeback or CBO of the buffered line drops it; a write miss that
  takes it merges at the install; the buffer is taken through F_WB since the victim may be
  dirty; the L2 port stays single-request). `workloads/membench` (new, 1 MiB streams): fill
  92.8 -> 60.7 cycles per line, sum 75.7 -> 43.4, copy 156 unchanged. tiny128 boot +34.3% at
  60 M cycles (the memory-init walk). tb_ooo2_dcache T18-T20. Board: build P.
- 2026-09-05: build N (4a+4b at HW=8) failed timing, WNS -0.576: the combinational D$ accept
  fed the LSU's completion, M's `done` and the scheduler in one cycle (22 levels), and the
  placer then scattered the PRF-read -> address-add -> store-queue snoop path (5.3 ns of
  route). The accept is registered now, the LSU takes the next queued store from S_ST on it
  and the queue pops at the handoff: stbench still 2.31 per store at HW=8; the boot pays
  0.85% (an isolated store frees the LSU a cycle later), the price of closing. The first cut
  computed the store mask from the queue head's size, which had moved on -- an `sd` went out
  with a one-byte mask; riscv-tests caught it in 140 tests, and the mask uses the registered
  size now. Build O carries items 4c, 6, 7, the redirect-drain counter and the HW=8 default.
- 2026-09-05: build O (everything through the registered accept, at HW=8) closed at WNS
  +0.001 after post-route physopt (routed -0.050) and booted to login with 0 faults -- then
  faulted under load: a python segfault at 479 s, a kernel Oops in the dentry LRU (a list
  pointer read as 3) during a drop_caches at 714 s, NFS RPC timeouts during 100 MB reads;
  M's session had none. sha256sum on O: IPC 0.744, redirect drain 1.6% of cycles (r0317).
  tb_virtio_net's new 200-frame stress (random lengths 14-1514 and alignments, TX batches,
  RX drops, jittered AXI) finds nothing in item 7. Bisecting on the board: P1 = O without
  item 7. The
  Geekbench image to 400 M cycles on that tree: 110,583,456 retires. Tried taking the S_FIN
  door's set compare off the accept cone (take the request, let `b_live` send a same-set
  read back through S_LOOK): -5.0% on the boot and 2.31 -> 2.43 per store, because stack
  traffic makes a same-set access after a store the common case -- reverted. If the door
  cone ever fails timing, the next option is a bypass: register the collision flag and the
  written chunk at the accept and substitute them for that bank's read data in S_CHECK.
  The D$ bench's `do_store` now drops its request after the registered accept, as the LSU
  does; held to the ack, the open door took the same store twice.
- 2026-09-05: virtio-net now re-reads avail.flags after writing used.idx before deciding
  the interrupt (virtio's notification-suppression rule); the old device sampled it at the
  start of a delivery, a race that a 30 k-cycle delivery hid and a 4 k-cycle one exposes as
  lost wakeups (NFS RPC timeouts on O). Bench cases toggle the flag mid-delivery. Whether
  this is O's whole story, P1 (O without item 7) and P2 (O with this fix) will say.
- 2026-09-05, the bisection: P1 (O without item 7) FAILED the gate -- "scheduling while
  atomic" in systemd-networkd, a netplan segfault at 0x111 -- so the core, not the device,
  corrupts. P3 (P1 with the D$ stream buffer off) PASSED, login and 0 faults. Item 6 is the
  culprit and is OFF (`PREFETCH(0)`); the RTL, its rules and T18-T20 stay for the day the
  mechanism is named. The DMA-blind cosim and the random cache stress could not see it: the
  next instrument is a system-level DMA agent in the cosim (plan item 9's precondition).
  Expectations back to 14,057,886 (HW=8) / 13,732,722 (HW=4). P4 = P3 + item 7 with the
  interrupt fix, to judge the device on a clean core.
- 2026-09-05, P4 = P3 + item 7 with the interrupt fix (= main at 9c50490c): gate PASS,
  login and 0 faults; the stress clean (3 x 100 MB NFS reads, sudo and python3 hashes equal
  to the host's, 10 min idle, 0 console faults); sha256sum IPC 0.741. So the device is
  validated on a clean core, and the fault of O/P1 was item 6 alone. But a 100 MB cold NFS
  read still takes 189 s (P3, old device: 193 s): the device was never the limit. Measured
  on P4 during a 30 MB read (19 s): the device delivered 12,032 frames, the kernel received
  12,037, the engine busy-dropped 20, no-buffer drops 1, DMA errors 0 -- and the HOST
  retransmitted 1,939 of its 8,046 segments (24%, all SACK fast recovery, no RTO), the board
  queued 3,553 out-of-order segments, ping RTT 0.4-0.6 ms, CPU 45% busy / 55% iowait. The
  kernel's rx_dropped (20%) is LAN multicast/broadcast chatter (33:33:..., 01:00:5e:...,
  sampled from the device's last-RX head words; tcpdump is not built into this kernel).
  The loss is between the server's NIC and the board's kernel. [CORRECTED, same day: this
  entry first blamed "the board's 100 Mbit port". The link is 1000BASE-T -- d3b05896 recorded
  it, and the MAC cannot run slower: rgmii_rx's MMCM is built for 125 MHz in (its VCO would
  sit at 250 MHz on a 25 MHz rxc, under the 800 MHz floor) and eth_mac_rx wants a byte per
  clock. The FCS good/bad pair also exists (overlay word 18) and vnet-dbg.py never read it.]
  Hypothesis, from eth_rx_engine: the single buffer is released by the backend's ack after
  the whole frame is DMA'd (191 gather+write round trips for a full frame, plus 9 ring round
  trips), which is at least the 12.3 us a 1518-byte frame takes on a 1 Gbit wire; when the
  ack lands MID-frame the capture restarts at wptr=0 and the frame's TAIL is delivered as
  a good frame (FCS is the MAC's, computed over the whole frame). That is invisible to the
  busy-drop counter (only a frame that ENDS while held is counted -- the 20) and would be
  the kernel's 20% rx_dropped (random ethertype, no handler) and the host's 24% retransmits.
  Confirm on the board without a rebuild: overlay word 18 before/after a read (few FCS-bad
  expected), and the kernel's rx_dropped rate with vs without a read in flight (a LAN-chatter
  rate is independent of the read; a truncation rate tracks it). The fix is an RX engine
  that captures only frames whose SFD arrived while free, counts every frame it declines,
  and holds more than one frame -- before the burst master, not after.
  Instruments left in the scratchpad: board-stress.sh, board-perf.sh, vnet-dbg.py (the
  0x10003f00 debug overlay: words 20/21/22 = RX delivered / no-buffer / engine busy-drops).
- 2026-09-05, sha256sum on P4 in the stdin form (`sha256sum < /usr/bin/emacs`, Tommy's run):
  IPC 0.796, CPI 1.256, 714.5 M instructions; the file-argument form is 765 M and 0.741 --
  the 50 M are the read path and its serializing stall (5.1%) goes with them. The run had
  only the FE_* events, so "issue / retire" (90.3%) hid every backend stall: the tool now
  prints them as unattributed (10.7% there, -0.1% with the full set). Where the 0.256 CPI
  above the floor is (full set, bitstream O, same core): F/X queue empty 0.112 (8.3%), other
  frontend 0.032, LSU 0.125 (9.3%: 0.97 cycles per D$ access at 0.73% misses -- hit latency,
  item 9), serializing 0.069 in the file form and ~0 in the stdin form, ROB full ~0.003, the
  drain 0.015. The 8.3% is NOT the queue's own cycle: `d_valid` loads from `q_cnt`, so a
  bundle pushed at t reaches X at t+2, and any cycle the aligner produces nothing surfaces
  at X two cycles later -- when the window has bytes again -- and is charged to FE_QUE
  (FE_IC/FE_ALN only see gaps longer than the queue's occupancy). Hypothesis for the gaps:
  the fetch buffer runs ONE chunk ahead, requested at the slide (PC enters chunk1), and the
  I$ round trip is about what the aligner takes to consume a chunk. febench says exactly
  this: 4 whole 32-bit ops per chunk 0.98, 3 whole + a straddler 0.79 (one cycle per chunk),
  8 compressed 0.99. Compiled code (45-60% compressed here) has both short chunks and
  straddlers; 8% is one lost cycle per ~2.5 chunks. Confirm in the febench testbench: a
  `mixed` block (random 16/32-bit, compiled-like) -- expect ~0.90-0.92 with the loss in
  FE_QUE -- and the cycles from the slide to chunk2's arrival in the `shifted` block --
  expect ~4; 2 would refute it. Fix: request chunk n+2 as soon as chunk n+1 lands (a
  three-chunk buffer, still one request in flight), or a line-sized buffer that requests the
  next line on entering its last chunk. Expected: FE_QUE 8% -> 1-2% (redirect restarts),
  IPC 0.80 -> ~0.85; item 9 for the LSU's 9% after that. 1.0 is the ceiling at IW=1
  (`hpm_ret_q` is one bit); past ~0.9 is item 10.
- 2026-09-05, the experiment. febench gained a `mixed` block (half 16-bit, half 32-bit by a
  fixed LCG): 0.98, so random straddles cost nothing and the first hypothesis was wrong as
  stated. `workloads/shabench` (new: OpenSSL's C sha256 shape, bare metal with the board's 13
  counters self-programmed and printed in perf-stat format, plus a Linux build and `shalib`,
  which calls the board's own libcrypto) put the same code in the sim: IPC 0.898, `FE_QUE`
  8.0% with nothing else in the way -- the board's `sha256sum` number (five clean runs
  0.793-0.805, `FE_QUE` 8.2-8.5%; the pure C loop on the board 0.876, `FE_QUE` 4.0%; libcrypto
  under `openssl speed` 0.784, 8.4%). `FB_TRACE` (new, `rv_soc_top`) over 20 k cycles of the
  hash: slide-to-arrival is 3 cycles (4,205 of 4,209), 1,851 of 1,955 aligner gaps are a
  straddler holding one halfword, and 1,835 of 3,342 four-instruction chunks paid one cycle:
  the chunks with three whole 32-bit ops and a straddler, which is what compiled code is
  between its compressed instructions (sha256_blocks: 14% compressed). Fix: chunk2 is wanted
  on the slide cycle itself (`fb_wantv`'s fourth term), so it is usable when that straddler
  needs it. Sim: febench shifted 0.79 -> 0.98, shabench 0.898 -> 0.951 with `FE_QUE` 0.0%;
  what remains there is ROB-full 4.4% and the LSU 2.9%. Board: build Q (below).
  Two things seen on the way and not chased: the board's `sha256sum` twice read 0.71 with an
  LSU-dominated stack that did not close (all later runs 0.79-0.80), and the net device
  raised 1,619 interrupts for 141 frames in five idle seconds (about eleven per frame) --
  variable kernel work that inflated one run's instruction count by 68 M. Bracket a
  measurement with /proc/interrupts deltas (the `br()` helper in the session log) until the
  device is fixed.
- 2026-09-05, build Q = 644f732c on the board (tools/gate.sh: benches, WNS +0.001 on the
  scheduler-pick -> PRF path as before, login, 0 faults; bitstream banked as
  /var/tmp/ooo2_Q_fb-slide_166MHz_644f732c.bit). Bracketed by interrupt deltas, P4 -> Q:
  `sha256sum < emacs` 0.793-0.805 -> 0.855-0.872 (`FE_QUE` 8.4% -> 0.4%; what is left: LSU
  6-8%, ROB-full 5-6%, serializing 3.7%); the C loop (`shabench 64`) 0.876 -> 0.900-0.907;
  libcrypto under `openssl speed` 0.784 -> 0.820. Item 2 is closed: the fetch path costs
  1.2% of sha256's cycles now (FE_IC 0.7%, FE_QUE 0.4%, FE_ALN 0.1%), and the next sha256
  cycles are the LSU's and the ROB's -- items 9 and 10.
  Open, seen three times in ~25 runs on P4 and Q: a run at 0.71-0.74 with the SAME
  instruction count and D$ miss rate but LSU 36% and ROB-full 22% overlapping -- long loads
  that are not misses. Hypothesis: the dTLB (16 entries, direct-mapped on VPN[3:0]) with
  the stack page (X[]) and the K256 page on one index, a walk per load for the whole run;
  ASLR moves the stack per run, so it would be a 1-in-16 layout. `shabench` now prints its
  stack and K256 indexes; 8 random layouts + 4 fixed ones all ran 0.900-0.907 and none
  collided, so the hypothesis is untested, not refuted. A dTLB-walk HPM event (the LSU's
  MMU walking pulse, a 25th bit) is the instrument that would settle it in one run.
- 2026-09-05, item 7, the RX engine. Confirmed on the board around a cold 117 MB NFS read
  (223 s, 0.52 MB/s): the device delivered 112,933 frames, the kernel received 112,952 and
  DROPPED 23,347, the host retransmitted 23,528 segments, FCS-bad 0, engine busy-drops 45.
  Drops and retransmits agree to 1%: the single-buffered engine's ack landed mid-frame, the
  tail was captured from offset 0 and committed as a good frame (the FCS is the MAC's), the
  kernel discarded it, the server resent it. `src/eth_rx_engine.v` is now a ring of eight
  2 KiB BRAM slots: a frame is captured only if its FIRST byte finds its slot free, the
  decision is never revisited, every declined frame is counted (busy / FCS-bad / too long;
  overlay word 26 = {oflow, bad}, 22 = busy), and a burst of eight is held while the backend
  drains. The read is registered, so virtio_net presents the byte it consumes NEXT cycle
  (`rx_nxt`); the vnet bench's stub is registered too. `src/eth_rx_engine_tb.cpp` (rewritten,
  run by the new gate-visible `ooo2/run-ooo2-ethrx-tb.sh`): a burst, a full ring, the ack
  landing mid-frame (D: declined whole, no tail), FCS-bad, over-long. VNET-TB unchanged at
  rx=4018 (that figure, not 3,953, is what the interrupt-flag re-read left). Also measured:
  the device raises exactly one interrupt per RX delivery and one per TX completion (27,780
  for 16,869 + 11,146 over a 10 MB read), so "eleven per frame" was a miscount of frames; the
  cost that remains is one interrupt per ACK the kernel sends, which VIRTIO_F_EVENT_IDX would
  let the driver suppress -- a later step. Board: build R.
- 2026-09-05, the dTLB instrument (branch wip/dtlb-hpm, board: build S after R): `DT_WALK`
  r0318 (cycles the data MMU is walking, a subset of `ST_MEM`) and `DTLB_MISS` r0104 (walks
  begun), bits 24/25 of `hpm_ev`; the tool prints the walk share under the LSU line and
  walks per thousand with the cycles per walk; `perf-smol.sh mem` carries both. Decides
  the open bimodality (0.71 with LSU 36% and no extra D$ misses) in one run, and says on
  every workload what a 16-entry direct-mapped dTLB costs before anyone sizes a bigger one.
- 2026-09-05, gate U (main = the registered ALU writeback on 10a): the first two attempts
  never reached timing. tools/ram-manifest.txt still named `q_dat`, which 10a split into
  the parity banks `q_dat0`/`q_dat1`, so the rule-I7 check failed the build six minutes in
  -- both banks ARE LUTRAM -- and tools/gate.sh printed that as "FAIL (timing)"; the first
  attempt was then misread as memory pressure from a sim chain. The gate now names the
  stage (ram-inference / build / timing), the manifest follows the banks, U runs again.
- 2026-09-05, gate U (main: two-wide fetch + the registered ALU writeback), the first build
  of 10a: FAIL, WNS -0.620 ns on 6,327 endpoints of the core clock (TNS -1962), plus -0.195
  on 646 endpoints of the 333 MHz domain, at 32% LUTs and 49% CLBs (not congestion by
  utilisation). The worst family is the two-wide fetch path: pc_q -> iMMU -> fetch buffer ->
  aligner -> `+2*consumed` -> the RAS write data and the F/X queue's slot-1 PC, 23 levels
  with ten CARRY8, three quarters route (6.5 ns of 6.0). Two more families sit at the same
  -0.61 (the store queue's conflict bit into the integer scheduler's ready bits; the load
  queue's store tag through the issue select into `ps_out`), both of which closed at +0.001
  on builds Q/R -- read as the router's collateral until the frontend is fixed. Gate V
  (10b-10d on the same frontend) was stopped at synthesis: it could not pass. The fix: the
  fall-through and the slot PCs are a mux over `pc_q + 2i` (i = 0..2*IW, from the register,
  ready a nanosecond in) selected by the aligner's count and offsets -- no adder after the
  aligner; cycle-exact by construction, gated by the tiny128 count matching exactly. gate.sh
  now prints the real worst path (it printed a debug-hub path with 11 ns to spare before).
  Two traps on the way: the riscv-tests build the core at HW=2 (tb_ooo2_riscv.v's own
  localparam) while the cosim builds the shipping HW=8, so a select whose loop index ran
  to 2*IW truncated to the 2-bit count there (4 read as 0, every PC came out +8, 240
  timeouts) while the 60 M-cycle cosim was exact -- the sums run to min(2*IW, HW) now, and
  a width-dependent bug can hide in either bench; and src/tb_fetch.v fails five of its
  checks on main BEFORE today's change (stale pc/seq expectations at a chunk boundary), so
  it cannot judge a fetch change until it is fixed or retired.
- 2026-09-05, gate U4 = ac5fe12f (main: two-wide fetch, the registered ALU writeback, the
  fetch-path selects): timing met at +0.039 ns, board PASS (login, 0 faults). From -0.620
  to +0.039 by taking the adders off the aligner's output; the margin now sits on one
  family, the load queue's store tag through the issue select into the issue register's
  source tags (19 levels, 71% route), the redirect target (26 levels, seven CARRY8) and the
  F/X queue data, at +0.039..+0.041. That is the scheduler-is-the-critical-path family, and
  it is what 10b-10e will be measured against next (gate V2 on the rebased branch).
- 2026-09-05, build U4 on the board (two-wide fetch, one-wide dispatch): `sha256sum <
  /usr/bin/emacs` at 0.856-0.871 IPC on quiet, interrupt-bracketed runs (Q: 0.86-0.87), i.e.
  neutral, as 10a alone was expected to be -- the queue reads empty 0.4% of cycles, the
  frontend bubble 1.5%. One run in eight was the bimodal case again: 0.727 with the LSU
  36.7% and ROB-full 18.2% of cycles. The `mem` set over six quiet runs did not catch a
  slow one but showed the dTLB walk rate itself varies by run, 0.49 to 1.80 walks per
  thousand instructions at 18.4 cycles each (0.7% D$ misses, 8.1% loads, 2.5% stores in all
  of them): page placement against a 16-entry direct-mapped dTLB, which is the standing
  explanation for the slow runs and now has a number to test against -- a slow run's walk
  count. A 2-way or 32-entry dTLB is the fix; not IPC-plan work until it is caught.

||||||| parent of 6ce0595a (dispatch: two instructions per cycle (plan item 10b))
- 2026-09-05, item 10b in simulation (branch wip/iw2-disp). Slot B dispatches beside A when
  both are plain ops (no serializing, fence.i, CBO, AMO, CSR, illegal, fault or interrupt
  pseudo-op on either side), B's scheduler differs from A's (one write port per scheduler
  and per payload memory: psmem/plmem are now one array per scheduler), at most one load
  and one store between them (a load in B behind a store in A captures tag+1), the ROB has
  room for two and nothing is being redirected. Rename port B: two copies of the speculative
  map with a newer[] bit, the intra-pair bypass, a second free-list read per shard;
  ooo2_pending takes B's allocation and six more queries; the frontend's second IR shifts
  into A when only A goes. Every new structure was first landed dormant and proved
  cycle-exact (14,136,876 = 14,136,876), then enabled: lint clean, riscv-tests 240/240,
  cosim lockstep-clean at +0.22% on the boot (14,168,448), sha256 kernel 0.951 unchanged
  (80% ALU: a pair of ALU ops still waits for the one ALU -- 10d), febench compressed 1.00,
  mixed 0.99. The boot's remaining d_hold is src_pend-free by construction now; what holds
  it is rob_full (5.6 M of 60 M cycles) -- which is 10c's number to move.
  The Geekbench image to 400 M cycles: main (S) 102,054,588 retires, 10a 102,214,267
  (+0.16%; the row is now in cosim-expected.txt, per-batch); a kernel boot is memory-bound
  and the fetch side was 1.2% of it, so 10a's value there is the frontend 10b needs.
- 2026-09-05, item 10c in simulation (branch wip/iw2-disp). The entry behind the head
  retires in the same cycle when it is done, never when M still holds its op (a trap or a
  redirect waits there for the head with its done bit set) and never in a flush cycle (a
  mispredicted branch commits and redirects at once; the fall-through of a taken loop
  branch retired behind it before that rule -- 109 of 240 riscv-tests, every -v test).
  Rename's committed map is two copies with a newer bit, the free lists are two parity
  banks (two pushes, two pops, one write per bank), the shadow regfile and the cosim probe
  take two records in order, minstret and the HPM count both. riscv-tests 240/240, lint
  clean, cosim lockstep-clean at +1.21% on the boot (14,308,187), sha256 kernel 0.951 ->
  0.965 with ROB-full now 11.2% of its cycles (LSU 1.0%): the 16-entry ROB is the next
  wall, and the ALU (one) after it. febench unchanged. Board: with 10b, after 10a's T.
||||||| parent of 8a5e6fcd (rename, rob: one write statement per LUTRAM bank -- gate V2 lost every parity bank to flops)
- 2026-09-05, item 10e in simulation (branch wip/fe-runahead, on 10d-ii): the fetch buffer
  runs two chunks ahead and the aligner waits instead of cutting. A third chunk slot that
  only feeds slot 1 at the slide; two request entries, the I$ port's tag naming the entry
  and the answer's address the slot (both asserted); the request for the first of
  chunk0..2 neither held nor in flight, chunk1..3 in the slide cycle, chunk3 alone after a
  short forward jump into chunk2 (a two-chunk shift now, not a realign); shifts chosen by
  address, so the PC entering a chunk still in flight keeps the others -- and the pa/va
  pair tagged valid only when captured under a real translation (`fb_tagv`: the first cosim
  caught a mid-walk leaf's PA, 0x40, kept under the VA and asked for). The aligner's
  chunk-boundary cap on slot 1 is gone: a slot whose bytes are short of the page end waits
  for them (`bytes_late`), so a bundle's shape is still the code's. riscv-tests 240/240,
  lint clean, sha256 kernel 1.171 -> 1.573 IPC (7.75 M -> 5.77 M cycles): the F/X queue
  empty 22.5% -> 0.5%, and the wall is the ROB now -- full 15.9% of the kernel's cycles, the
  LSU 5.8%; with the two-per-cycle floor at 78.6% that is every cycle accounted for. The
  model held: an FB_TRACE window inside the kernel shows the as-built buffer emitting a
  bundle in 74.6% of cycles (the model said 75.1%) and the run-ahead one in 99.0%, a slide
  every 2.7 cycles, 0.6% of cycles without bytes. Cosim lockstep-clean at 60 M cycles,
  14,441,735 retires: -0.48% against 10d-ii on the boot, which is I$-miss-bound -- two
  chunks of run-ahead past a taken branch are two wasted fills there, and a bundle now
  waits for late bytes where it used to cut (the Geekbench image will say which). Board
  after the frontend timing fix on main (gate U's -0.620 was 10a's own adders).
- 2026-09-05, a 32-entry ROB on 10e (branch wip/rob32, one localparam): sha256 kernel
  1.573 -> 1.733 IPC (5.77 M -> 5.24 M cycles), ROB-full 15.9% -> 6.4%, the LSU 5.8%
  unchanged -- with the two-per-cycle floor at 86.6% of the kernel's cycles, that is
  within 2% of everything accounted for. The 2026-09-05 experiment on the frontend-bound
  core gave +1.4% here; with the frontend fixed the window is worth 10%. The boot (-0.4%
  then) and the Geekbench image (+0.55% then) are re-measured in the background; timing
  (the head/done scan grows with the depth) decides on the board, after the frontend fix.

- 2026-09-05, gate V2 (the rebased two-wide/two-ALU/run-ahead branch): FAIL at the rule-I7
  RAM-inference check, six minutes in, and this time the verdict said so: every parity bank
  of the free lists (fl_ie0/1, fl_ld0/1, fl_fe0/1, fl_i20/1) and the ROB's entries had become
  flops. Each bank was written from two statements (the head's free and the second's, which
  land in different banks by parity -- but synthesis sees two write ports). Fix: one {we,
  addr, data} per bank, muxed from the two frees; the ROB's entries became the same two
  parity banks (tail and tail+1 differ in parity) instead of flops. riscv-tests, the
  cycle-exact cosim, then gate V3.
- 2026-09-05, gate V3 (8a5e6fcd, the rebased branch with the bank fix): FAIL, WNS -0.919 ns,
  10,679 failing endpoints (TNS -5381) at 38% LUTs / 56% CLBs (U4: 32% / 49%; LUTRAM 6.6k
  -> 9.3k from the seven-port PRF). Six of the ten worst paths are one chain: the load
  queue's store tag -> the store queue's conflict compare -> the LSU/MMU port arbitration
  -> `ld_land` -> M's completion -> the redirect -> `d_take` -> rename port B -> the pending
  table's queries -> the schedulers' entry writes: 34 levels, 6.9 ns, 73% route. Dispatch
  was gated on the SAME-cycle redirect, so M's whole completion cone sat in front of every
  dispatch-side write. The other listed paths are U4's families (the fetch path from the
  interrupt inject, the M port's PRF read into m_result) about a nanosecond of route worse
  -- the bigger core placed worse. Fix, first: the redirect gates no same-cycle enable.
  Dispatch, the three issue takes, and the ROB, scheduler, pending-table and load-queue
  allocations proceed in the redirect cycle; an instruction taken then is younger than the
  redirecting op and dies with everything younger, because each structure's flush arm is
  ordered after its allocation and wins (rule I11). The store queue's "no allocation in a
  redirect cycle" assertion goes with it. Cycle-exact by construction; then gate V4 says
  what the route growth costs on its own.
||||||| parent of 21ac5b69 (core: two more late enables off M's completion -- dispatch holds on the registered fr_v, fetch's window on a registered context change)
- 2026-09-05, the Geekbench image to 400 M cycles on the rebased branch (10b-10e with the
  redirect fix, 6cf6b7b9): 103,501,912 retires, lockstep-clean, +1.26% over 10a's
  102,214,267 -- the kernel boot is I$/D$-miss-bound and two-wide dispatch has little to
  do there; the ROB-32 tree runs next. The number that matters is Geekbench's own score
  on the board, which waits on gate V4's boot.
- 2026-09-05, gate V4 (6cf6b7b9): timing met at exactly +0.000 ns (38% LUTs, 56% CLBs);
  the board did NOT boot -- programmed, no monitor banner at all, the payload upload's
  first sector never acknowledged. U4 re-programmed from its banked bitstream boots to
  login, so the board, console and upload path are fine; V4's core is what does not run.
  Nothing had ever simulated the ROM monitor on this core: tb_ooo2_linux now takes
  `-DTB_RESET_PC` and `-DSOC_BOOT_HEX` (the monitor baked into the SoC's SRAM as on the
  platform), which needed two real fixes on the way -- the LSU aligned only DRAM
  addresses and sent the local SRAM's byte loads to the D$ as spans (they worked on the
  board through the D$'s span path), so the LSU takes the SRAM's base and size as
  parameters and aligns it like DRAM; and the D$'s no-span assertion is scoped to cached
  requests. With that, BOTH main and V4 boot the monitor to its prompt in simulation
  (1,125,772 and 1,125,775 retires at 3 M cycles). So the failure is hardware-only, and
  the prime suspect is the 0.000 ns. The worst paths: M's address register -> the LSU's
  reservation logic -> the CSR/trap decision -> `mmu_flush` -> the iMMU's context change
  -> fetch's window validity -> the F/X queue's data pins (20 levels, 76% route), and the
  M port's PRF read (address fan-out 387) -> address/compare -> `m_result`. First lever,
  by the standing rule: NF 8 -> 5 (gate V5); if that does not boot, a 111 MHz diagnostic
  build separates timing from logic, and the M-completion-to-fetch chain gets a register.
- 2026-09-06 13:40, THE SILENT BOARD, RESOLVED. V4, V7, W2 and W2M (cdf30cb0 rebuilt from the
  main checkout, +0.009 ns -- the worktree correlation was a coincidence) printed nothing
  while riscv-tests, the cosims and the monitor boot in tb_ooo2_linux passed. The netlist
  answered where the board could not: rv_soc_top synthesized out of context with the build's
  options and defines and its funcsim netlist booted under xsim (tools/netlist-boot.sh,
  ooo2/tb_ooo2_netlist.v) retires 45 instructions in the first 1000 cycles and wedges; the
  RTL on the same testbench prints the banner by cycle 6000. Retiming, control-set and FSM
  options change nothing. A per-cycle port probe of the renamer, PRF, ALU and ROB on both
  (-DNETLIST_PROBE, tools/cmp-probe.py) put the first disagreement at rename: the netlist's
  port-A destination tag is `r_prd[6:0] = <const0>` -- Vivado kept ONE read port for the
  function `rd_ie(i) = i[0] ? fl_ie1[..] : fl_ie0[..]`, gave it to the last call site (port
  B) and folded port A's call to zero, so every instruction through port A wrote physical
  register 0. Synthesizing ooo2_rename alone (one minute per variant) proved it: swapping
  the call order moves the zero to port B; wires or split arguments change nothing; the
  reads as continuous assigns at module scope (one per port) keep both. Rule F4 (no function
  reads a RAM array) and F5 (the netlist boots the monitor before a bitstream is built; the
  stage is in tools/gate.sh, ~6 minutes) are in docs/rtl-rules.md. The fixed tree: lint
  clean, 240/240, cosim lockstep-clean at 14,308,077 retires (+1.21% over two-wide fetch,
  the row raised), and its netlist prints `smolrv64 monitor rtl=cdf30cb0` by cycle 3560.
  Gate W2F (319cd248, 14:19): the netlist stage passed in the gate itself, timing met at
  +0.002 ns, and the board booted Ubuntu -- login 1, faults 0. main is fast-forwarded to it
  and the bitstream banked as /var/tmp/ooo2_W2F_iw2disp_166MHz_319cd248.bit. Items 10b and
  10c are on the board. The full stack (wip/fe-runahead + the same fix in its four-shard
  renamer) is 240/240 and lockstep-clean at its own row; its remaining question is 10e's
  ld.so death (W1), which the fix does not touch.
  Board, 319cd248, `perf stat -e cycles,instructions sha256sum` on a 30 MB random file in
  /tmp (tmpfs): three quiet back-to-back runs 0.908 / 0.898 / 0.896 (2.126 G instructions,
  2.34-2.37 G cycles), the `perf-smol.sh cpi` set 0.89, an NFS-cached copy 0.89 -- and 0.81
  once with something else running (2.22 G instructions: interrupt work lands on the task).
  Tommy measured 0.85 twice by his own method, whose U4 number is the 0.86-0.87 in the spec;
  the two methods are not the same yardstick, so no delta over U4 is claimed here until one
  method is run on both bitstreams. Note the DTB's `model` string names the tree that last
  regenerated the DTS (8dbf7eff here), not the bitstream: the monitor banner's `rtl=` is the
  truth (319cd248).
  Rule F4 is mechanical now: tools/check-func-ram-reads.py runs in src/lint.sh, and the
  two map-read functions that happened to survive synthesis are written out per reader
  too (lint clean, 240/240, cosim 14,308,077 = the row, netlist banner).
- OPEN (2026-09-06): the W2 RTL under 4-state xsim (tb_ooo2_netlist.v -DRTL_RUN, no XPM)
  dies at cycle 51 with "ooo2_rob: completion for slot 8, which holds no live entry" -- a
  rob_w_valid for entry 8 the cycle after entry 8 retired; ft_rob and sq_kc_rob are X there.
  Verilator's zero-init hides it and the netlist (INITs) does not show it, so it is either
  an X artefact of a register that only `initial` sets, or an ordering hazard that a
  zero-initialised register masks. Worth a Verilator run with --x-initial unique.
- 2026-09-06 15:40, the stack rebuilt on the board-validated base. wip/w3 = main + 10d-i +
  10d-ii (the full stack's core, pending table, PRF and renamer with V6/V7's late-enable
  fixes; the frontend main's): F4 and lint clean, 240/240, cosim 14,511,093 (+1.42% over
  W2F, the row raised), netlist banner; gate W3 running. wip/w4 = w3 + 10e + the FBDIAG
  arrival latch + the cache bench + the frontend tools: its RTL is the full stack's (plus
  the latch) with the renamer fix, F4 and lint clean; riscv-tests and the cosim queued
  behind W3's Vivado. W4's gate is the diagnostic run for 10e's ld.so death: init dies,
  the self-reset brings the monitor back, and FBDIAG says whether the arrival bypass ever
  served a stale mapping.
- 2026-09-06 15:50, gate W3 (b027f065 = main + 10d-i + 10d-ii with V6/V7's enables, the
  frontend main's): netlist banner, then TIMING FAILED at -0.798 ns on the fetch family --
  `fb_va_reg -> ... -> pc_q_reg`, 18 levels, 6.79 ns of which 74% is route. The same family
  closed at +0.002 on W2F and at +0.016 on V7 (whose buffer is 10e's). So 10d is not gated
  on hardware alone: W4 (w3 + 10e + the FBDIAG latch, V7's shape) goes next, and item T1's
  fetch-stage cut (F) is what the family needs regardless.
- 2026-09-06 16:05, gate W4 (fac08ffd = w3 + 10e + the FBDIAG latch, V7's logic plus the
  renamer's replicated free-list reads): netlist banner, TIMING FAILED at -0.584 ns, and
  not one family -- the twelve worst endpoints are the F/X queue's data pins, m_result, the
  store queue's wb_q, ps_out0, the divider and the UART, all within 5 ps of each other at
  70% route: the placer lost about 0.6 ns across the whole core, which is T1's "the roll is a
  nanosecond wide" on a design that grew (the fourth shard, the second ALU, the LUTRAM
  copies the F4 fix asks for). V7's +0.016 was the lucky roll. So: the ld.so probe goes on
  W1's own shape (wip/w1 = old main + 10e + the latch, which closed at +0.032 as W1), and
  T1's fetch cut (F) starts now on main; 10d and 10e land on the board after it.
- 2026-09-06 17:05, gate W1D (8dbf7eff = W1 + the FBDIAG arrival latch, W1's shape that
  closed at +0.032): TIMING FAILED at -0.186 ns -- and the worst path is not the core's:
  `virtio_blk_backend/dma_master/w_pending_reg_replica -> probe_bridge/buf_data_reg[428]/CE`,
  6 levels, 2.87 ns of which 86% is route, in the wrapper's ui_clk domain (3 ns). Three
  consecutive builds of three shapes (W3 -0.798, W4 -0.584, W1D -0.186) on a chip at 56% of
  its CLBs: the core's growth is crowding the wrapper's placement too. The 10e probe waits
  for T1's margin; W4's and W1D's near-critical census (tools: `make census`) will say
  where the mass sits, by module and by clock, before the cut is chosen.
- 2026-09-06 17:40, the census of W4 (13,098 endpoints under +0.35 ns): 4,112 of them are
  ONE family, `fb_va_reg -> q_dat0/q_dat1` -- the fetch buffer's tag compare, hit mux and
  arrival bypass into the served window, the aligner, and the F/X queue's LUTRAM data pins
  (25 levels, 4.25 ns of route in 6). Next by mass: `u_sq/headc_reg -> e_r / ps_out / i_ent`
  (the store queue's commit pointer into the issue enables, family L, worst -0.633) and
  `ps_out_a_reg -> alu_q_val` (the ALU port's PRF read and execute, family W). W1D's census
  says the same with 10e's buffer: `u_lq/sqt_reg -> q_dat` (3,927 paths) and the L family.
  T1 (F), first step, branch wip/t1f (765b6704): the served window and its count are
  functions of register bits only -- `fb_pair >> {imem_addr[CHA] ^ fb_va[CHA],
  imem_addr[CHA-1:0]}`, the count from fb_v1 and that offset -- and the bytes' validity
  (fb_hit, fi_stall, the iMMU's ready/fault, the context gate) leaves as ONE late bit,
  `imem_ok`, that gates the bundle's valid and the straddle entry, i.e. the handshake, and
  nothing in the data or count cone. The arrival bypass is gone from the data path (a
  buffer miss costs the cycle the fill takes to land in its slot). Lint clean, 240/240;
  cosim, netlist boot and the gate follow. Cosim: lockstep-clean, 14,179,836 retires,
  -0.90% against the row (14,308,077) -- the arrival bypass's cycle, on a boot that is
  I$-miss-bound; the sha256 kernel, which hits, should not move. Whether that is paid
  depends on the margin the gate reports: a nanosecond of structural margin buys the
  whole two-wide stack, 0.9% of a miss-bound boot does not decide anything.
  Gate T1F (0151de8d, 18:20): netlist banner, TIMING FAILED at -0.442 ns. The census: the
  fetch family is GONE (no `fb_va_reg -> q_dat` endpoint under +0.35 where W4 had 4,112)
  -- and the F/X queue's data pins are still the largest mass, now reached from
  `irq_inject_q_reg` (3,673 paths, 20 levels, 4.1 ns of route): the bundle muxes
  (interrupt pseudo-op / straddle / aligner) and the slot-PC selects into 826 LUTRAM data
  pins across the chip. The worst slack is family L (`u_sq/headc -> ps_out CE / i_ent / e_r`,
  -0.676) and `ps_out0 -> m_result` (family W, 5.0 ns of route on 18 levels). Every family
  is 70-80% route: the placement is the problem as much as any cone. Next: (a) register the
  bundle before the queue write (the q_dat cone becomes register -> pins), (b) the L family,
  (c) a placement-directive sweep of the same netlist, since four builds of three shapes
  swung 0.8 ns on the placer alone.
  T1 (F) step 2, branch wip/t1f2: the bundle is registered before the F/X queue's write
  (`pb_v/pb_in0/pb_in1` in ooo2_frontend), so the queue's 826 data pins are driven by a
  register and the aligner, the interrupt/straddle muxes and the slot-PC selects end in a
  flop; fetch's handshake, the predictor's training edge and the interrupt pseudo-op's
  consumption move to the register's load; a redirect kills the register with the queue.
  Cost: one cycle of fetch-to-decode latency, seen only when the queue is empty. Lint
  clean; sims and the netlist boot queued behind the ExtraTimingOpt rebuild of step 1
  (a directive check, not a plan: build.tcl's c4134edd chose AltSpreadLogic_medium by
  measurement, "204 ps is not a sweep knob").
  Family L, read for the next step: every scheduler's take is `iq_iss_take = iq_iss_v &
  iss_ready` with `iss_ready = ~i_v | i_done` and `i_done = i_v & (m_advance | f_advance)`
  -- M's completion cone (the store queue's commit pointer, the load queue's ordering
  decision, the LSU's replay, the CSR's redirect) is the clock enable of every scheduler
  entry's ready bit and of the issue register. The cut: a two-entry issue queue whose
  `iss_ready` is a register (the queue not full), M pulling from its head on its own
  advance, so the schedulers never see m_advance. The complication: each scheduler holds
  its issued entry (`hold_v/hold_ent`, one entry) until it leaves the issue register, and
  the payload LUTRAM is read at the head's entry -- two held entries per scheduler is the
  interface change. Family W (`ps_out -> m_result`, the PRF read and execute in one cycle,
  5 ns of route on 18 levels) has only the register the plan already names, at a cycle of
  load-to-use; it stays last.
- 2026-09-06 19:40, the SAME T1F netlist (5e362445) placed with `PLACE_DIRECTIVE=ExtraTimingOpt`:
  TIMING MET at +0.057 ns, against -0.442 with the default AltSpreadLogic_medium -- half a
  nanosecond on the directive alone, on a design where c4134edd (2026-09-01) had measured
  the other way round. So the directive is a per-design coin with a 0.5 ns face, not a
  knob: the structural cuts continue, and every gate from here runs both directives when
  the first misses. Queued: T1F2 (step 1 + step 2) and W4 (the full stack) with
  ExtraTimingOpt; a W4 that closes puts 10d and 10e on the board while T1 continues.
  20:00: T1F with ExtraTimingOpt booted Ubuntu on the board -- login 1, faults 0, rtl
  5e362445 -- so T1 (F) step 1 is board-validated on main's shape (bitstream banked as
  /var/tmp/ooo2_T1F_fetchcut_ETO_166MHz_5e362445.bit). It lands on main with step 2 once
  T1F2's gate is in.
  sha256sum on it (same recipe as W2F's, 30 MB in tmpfs, three runs under the same
  post-boot load): IPC 0.896 / 0.907 / 0.895 -- unchanged from W2F's 0.896-0.908, as a
  kernel that hits the buffer should be; the cut's 0.9% is the miss-bound boot's.
- 2026-09-06 20:20, gate T1F2 (18547908 = step 1 + the registered bundle, ExtraTimingOpt):
  cosim lockstep-clean at -0.27% against the lowered row, netlist banner, TIMING FAILED at
  -0.201 ns. The census: 4,305 near-critical endpoints (W4 13,098, T1F 8,728) and NO
  queue-data family at all -- the two cuts retired what they aimed at, and the WNS moved
  -0.58 -> -0.44 -> -0.20 with them. What is left: `i_ent_reg -> u_pend/pend_reg` (462, the
  issue register's index through the payload LUTRAM into the pending table, 14 levels),
  `ps_out0 -> alu_q_val / m_result / wb_q` (family W: the PRF read and execute, 12 levels
  of which 4.7 ns is ROUTE), `u_sq/dv -> u_fpu/req_ops_q` (27 levels), and at +0.05 a pure
  fan-out family, `u_lsu/pa_q -> u_ddr_hpm/wrb_reg/R` (512 reset pins, 6 levels, 5.0 ns of
  route). Twelve levels needing 4.7 ns of wire says the register file's replicas sit far
  from M: the placement report (make place-report) is the next input, and a pblock that
  keeps the PRF, M and the ALU together is the cheapest experiment before family W's
  register.
  T1 step 3, wip/t1f2: the issue payload is read at pick, from each scheduler's own
  candidate, and registered with the tags (`pl_q`), so M's controls start from flops
  instead of a LUTRAM read at last cycle's entry -- the `i_ent -> u_pend / u_sq` families.
  Lint clean; sims and its ExtraTimingOpt gate queued behind W4-ETO's build. The pblock
  experiment is parked: with three cuts each retiring its family, the structural path is
  the one to finish before pinning anything.
- 2026-09-06 21:30, W4 with ExtraTimingOpt: -0.315 ns (the default gave -0.584). The full
  stack needs the cuts, not a directive: steps 1-3 are written against main's fetch buffer
  and land on 10e's buffer once they are validated on main's shape.
- 2026-09-06 22:10, gate T1F3 (ca457d95 = steps 1-3 on main's shape, ExtraTimingOpt): timing
  met at +0.002 ns, the board booted Ubuntu -- login 1, faults 0. main is fast-forwarded to
  it (the boot row at 14,179,836; step 3 is bit-identical to step 2 in the cosim) and the
  bitstream banked as /var/tmp/ooo2_T1F3_threecuts_ETO_166MHz_ca457d95.bit. The margin
  itself is still the placer's (+0.057 / -0.201 / +0.002 across the three steps with the
  same directive); what the cuts bought is the endpoint mass, 13,098 -> 4,305, which is
  what the full stack needs. wip/w5 (the full stack + the three cuts) is next: sims,
  netlist boot, its ExtraTimingOpt gate, and the ld.so probe if it boots.
  sha256sum on T1F3 (same recipe, post-boot load 5.4): IPC 0.908 / 0.894 / 0.870, the
  last with 0.8% more instructions (interrupt work) -- the 0.90 band of W2F and T1F; the
  cuts cost the hitting kernel nothing on the board either.
- 2026-09-06 23:20, gate W5 (ba15a225 = the full stack -- 10b, 10c, 10d-i, 10d-ii, 10e, the
  FBDIAG latch -- with the renamer fix and the three cuts, ExtraTimingOpt): cosim
  lockstep-clean (14,301,029, -0.97% against the 10e tree's own count: the cuts' cost),
  netlist banner, TIMING MET at +0.003 ns, the monitor and the kernel boot, and init dies
  at 2.39 s exactly as on W1: `unhandled signal 11 code 0x2`, epc == ra == 0x3fb9a78820,
  page offset 0x820, the word at epc a page-aligned pointer 0x28820 below it -- the same
  two numbers as W1 (0x3facd24820, 0x3faccfc000). Deterministic across two builds, two
  bases and the cuts; and NO self-reset followed: neither the hit path's nor the arrival
  path's stale-mapping latch fired, and on W5 the arrival bypass is not even in the data
  path. So 10e's defect is not a stale VA->PA; it is a code path, which means a simulator
  can find it if it runs the same code. Running: the glibc cosim (dash + coreutils through
  ld.so, workloads/glibc) on the W5 tree. Next if it stays clean: a workload that runs
  the board's own /usr/lib/systemd/systemd through ld.so under the cosim (its library
  closure copied from the NFS root, `systemd --version`), since the death is in the
  loader's work on that binary's libraries.
- 2026-09-07 00:10, THE LD.SO DEATH REPRODUCES IN LOCKSTEP. The glibc cosim (dash + coreutils
  through ld.so, workloads/glibc) on the W5 tree dies at cycle 833,573,569 with the fetch
  buffer's own invariant: "VA-matched arrival serves a STALE mapping: va=3f99a24000
  pa_now=82ba2000 arrived=82ba1000" -- an I$ response for a page-aligned user VA whose
  translation moved one page between the request and the arrival, with no context change
  poisoning the request in between. A page-aligned fetch VA in a user mapping is chunk0
  of a page the PC entered; and a translation that moves by exactly one page is what
  ld.so's MAP_FIXED remap of a library's data segment does (file offset - VA offset =
  -0x1000: the same VA goes from the whole-file mapping's page to the segment's, the
  next page cache page down). Rerunning with the fetch-buffer trace extended to the PAs,
  the request entries and the translation state (PLUSARGS hook in the cosim runner,
  +trace_from/+trace_to around the cycle) to see who put the PC there and why the
  request survived the remap. The systemd workload (workloads/sysd) stays as the second
  repro if needed.
- 2026-09-07 00:50, ROOT CAUSE of 10e's ld.so death, from the trace: at cycle 833573528 the PC
  is at the last chunk of a user page (VA 3f99a23ff0, PA 82ba0ff0); it falls through to VA
  3f99a24000, whose translation is 82ba2000. The run-ahead buffer's address-based slide
  matched the VA against its chunk1 (`fb_in1a`, VA-contiguous) and took the slide arm --
  `fb_pa <= fb_pa1` = 82ba1000, the next PHYSICAL page, tagged with the next VIRTUAL page
  -- then requested and served that neighbour as the new page's code. Virtual contiguity
  is not physical contiguity; the pre-10e buffer could not slide across a page because its
  slide required chunk1 valid, and validity implied the same-page fill. Kernel text is
  physically contiguous, so no cosim of the kernel or of static busybox could see it;
  ld.so's text is page-cache pages, so it died at the first page fall-through whose
  neighbour was elsewhere -- deterministically, 2.4 s into every boot. Fix on wip/w5:
  `fb_in1a`/`fb_in2a` carry `fb_samepg`/`fb_samepg2`, so a page crossing is a realign
  from the iTLB. Running: the glibc cosim to its end, riscv-tests, the boot cosim, the
  netlist boot, the gate and the board. The FBDIAG arrival latch did NOT self-reset the
  board on W5 although the sim's twin of it fired: open, low priority now that the cause
  is known.
- 2026-09-07 01:50, the fixed W5 tree under the glibc cosim: past the death (cycle 833 M)
  to the login prompt at 1.13 G cycles, no invariant, riscv-tests 240/0. TWO CORRECTIONS
  to the record above. (1) tiny128's busybox is a dynamically linked PIE, not static: every
  tiny128 boot runs ld.so from /init on (~830 M cycles in), so the "glibc" cosim's repro
  was tiny128's own userspace; the 60 M gate cosim ends 770 M cycles before any of it. (2)
  workloads/glibc has NEVER executed its test, on any core: the overlay cpio is appended at
  offset 3,301,923 and the kernel's initramfs loop accepts a cpio header only at a 4-byte-
  aligned offset (init/initramfs.c), so it stopped there -- the "Initramfs unpacking failed:
  invalid magic at start of compressed archive" line in every log was that, and the 1.8 G-
  cycle run of 2026-09-04 that "ran past the test unseen" had no test to run. Same in
  workloads/sysd. Fixed on wip/w5 (the first archive padded to 4). Queued behind the gate:
  the glibc harness (expect "=== S98glibc ===" and GLIBC-TEST PASS) and the systemd one.
- 2026-09-07 02:05, the fixed tree's verdicts so far: the 1.4 G-cycle tiny128 boot through
  ld.so clean to the end (452,268,954 retires), riscv-tests 240/0, the 60 M cosim
  14,301,801 (+772 over the unfixed W5; the row is re-based to it from the 10d tree's
  14,511,093: the T1 cuts' extra register stages cost 1.44% on the miss-bound boot, the
  price of +0.3 ns of slack), the netlist boots the monitor. Committed on wip/w5: the
  harness padding (4ef31dd0), then the fetch-buffer fix with rule D10 (3ea93ec6). Gate
  W5fix (ExtraTimingOpt) launched 01:59 from the tree at 4ef31dd0 with the fix already in
  its RTL; 3ea93ec6 adds only the cosim row and the docs. The board is the verdict.
- 2026-09-07 02:55, gate W5fix FAILS TIMING at -0.271 ns (ExtraTimingOpt; W5 was +0.003):
  the worst path is the frontend's PC loop, irq_inject_q -> the fetch VA mux -> the iMMU's
  VA match (LUT + 3 CARRY8) -> the served window's bit 14 -> the straddle detect ->
  pb_two -> lenp/pc_q[41]: 20 levels, 6.33 ns, 66% route. The fix's own term is in that
  cone: `fb_samepg` was `fb_pa1[63:12] == fb_pa[63:12]`, a 64-bit sum's carry chain and 52
  XNORs, and since the fix it sits in fb_hit -> imem_ok -> every aligner enable. Rewritten
  as what it means -- chunk0's index in the page against the page's last (`fb_ci != LASTC`,
  8 register bits, one LUT; likewise samepg2/3) -- and re-gated (chain: tests, the 60 M
  cosim, the netlist boot behind the census's Vivado, then ExtraTimingOpt). The census of
  the failed checkpoint runs first to say whether the PC loop is a family or one path's
  placement. Pending either way: the loop is fetch VA -> iMMU match -> imem_ok_g -> PC
  advance in one cycle; a served HIT already carries its translation (fb_tagv, dropped on
  a context change), so `immu_ready & ~immu_fault` may be redundant on the hit path -- the
  next fetch cut if the family stays.
- 2026-09-07 03:40, the census of the W5fix checkpoint (rows print twice; halve): 73 of 80
  families under +0.35 are NEGATIVE, all by about 0.27 ns -- the load queue's ordering
  decision (`u_lq/sqt` -> ld_older -> lq_b_early -> m_done -> iss_ready -> pl_q, 362
  endpoints; -> u_fpu/req_ops_q 192; -> pla_q 132), the frontend PC loop (`irq_inject_q`
  -> u_fetch 363, -> u_bp 278, -> pb_in 344), the CSR writes (`m_imm` -> mhpmevent 197, 10
  levels of which 4.7 ns is route), `a_ent` -> u_pend 426, `m_addr` -> the free lists,
  ps_out -> m_result/wb_q (W), cvfpu's own. W5 closed at +0.003 the day before on the same
  RTL minus one LUT; T1F3 at +0.002: the design sits at the edge everywhere and an
  ExtraTimingOpt build is a coin flip of about 0.3 ns. PBLOCK_CORE is a weak lever (the
  core's 69k cells need their ten clock regions). So, on wip/w6 (wt-w6, off wip/w5),
  structural cuts with no IPC cost, each an exact rewrite or a one-cycle-late conservative
  copy, all lint-clean, validation queued behind gate W5fix3:
  (F1) the fetch address is the bare PC register: `imem_addr = pc_q` in every state, a
  straddle steps pc_q to the high halfword and `ipc_q` keeps the instruction's PC; a
  pending interrupt waits out a straddle (`irq_go = irq_inject & ~strad`, exported as
  `irq_pres` for the frontend's irq_taken) -- irq_inject_q leaves the loop's head;
  (F2) the iMMU's verdict leaves the served hit: `imem_ok_g = imem_ok & ~imem_ctx_chg_q`
  -- a hit carries its translation (fb_tagv); asserted that a hit never meets a fault;
  (F3) the page cap `hw_cap` from register bits (the last HW halfwords of the page and
  what is left of them) instead of `(4096 - off) >> 1` compared with HW: two carry chains
  off the aligner's count; the sum form stays as the oracle;
  (L1) the older-store answer M reads for its early release is ooo2_sq's REGISTERED
  per-load copy (`l_older[gl] <= |oldm`, the vector l_block already computes) at M's own
  load index, not the live `sqt[acc] - headc` query; the set of older live stores for one
  load only shrinks, so the copy can only be the more conservative, and the live port stays
  as the oracle the core asserts against. Also tools/gate.sh NO_BOARD=1 (build, bank the
  bitstream in the evidence directory, stop) for a build whose board is running Geekbench.
- 2026-09-07 03:55, GATE W5FIX3 PASSES: the full two-wide stack (10a-10e, T1 steps 1-3,
  the page-crossing fix, the register-bit same-page tests; 9b3050f9 on wip/w5) closes at
  +0.011 ns with ExtraTimingOpt and the board boots Ubuntu to login with ZERO faults --
  the ld.so death is gone on hardware. Bitstream banked as
  /var/tmp/ooo2_W5FIX3_fullstack_ETO_166MHz_9b3050f9.bit. Geekbench (the plan's metric)
  is running on it (/var/tmp/gb5-w5.sh, raw log gb5-w5-raw.log); sha256sum measured first.
  wip/w5 lands on main next. On wip/w6 the L1 assertion fired in the first cosim: not the
  cut -- the check ran on M's stale load index while the queue's candidate was a load
  dispatched one cycle earlier, whose copy had not caught up and which M was not holding;
  the check now carries the same `m_ld_nb` term the early release does.
- 2026-09-07 04:05, THE BOARD'S FIRST NUMBER FOR THE FULL STACK: `perf stat -e
  cycles,instructions sha256sum` on the 30 MB tmpfs file, three quiet back-to-back runs
  right after boot (load 1.87): IPC 1.32 / 1.38 / 1.39 (2.119 G instructions in 1.53 G
  cycles) against 0.90 on T1F3 (one-wide dispatch, two-wide fetch) and W2F. Geekbench
  launched at 04:05 (gb5-w5.sh, ~6-9 h at the old core's pace).
- 2026-09-07 04:30, LANDED: main is the full two-wide stack (12029862 = wip/w5's fifteen
  commits rebased onto main, RTL byte-identical to the board build 9b3050f9 plus main's
  PBLOCK_CORE knob; the rebase's union resolution had dropped seven of main's plan entries,
  restored). The spec records the board's sha256sum IPC. wip/w6 (F1-F3, L1) continues off
  it; with the riscv testbench now serving bytes only under a translation (the SoC's own
  contract, which F2 relies on) the failing classes pass 42/42, and every "hit meets a
  fault" event the probe found was the context change's one-cycle window (asserted as the
  exclusion). Full W6 validation and a build-only gate are running.
- 2026-09-07 04:15, W6 validated: riscv-tests 240/0, the 60 M boot cosim 14,301,801 --
  IDENTICAL to W5fix3's count, the four cuts are cycle-exact on the boot -- tb_fetch,
  tb_fetch_pagecross, LQSQ-TB and LQSQ-RAND pass. Two relaunches of its gate went to the
  benches: the new `l_older` pin was missing from the two LQ/SQ benches (rule G4, twice in
  one hour: run every ooo2/run-ooo2-*-tb.sh by hand after a port change, before the gate).
  Committed on wip/w6 (the cuts with rule D11 and the spec; the tools). Gate W6 (build
  only, ExtraTimingOpt, evidence under gate-results/37769247/ -- the tree's HEAD when it
  started) is running; the board keeps Geekbench on W5fix3. Queued behind it: the corrected
  glibc and systemd harnesses on main.
- 2026-09-07 04:40, the board says what it runs: ubuntu-boot.sh regenerates ubuntu-nfs.dts
  with the monitor banner's `rtl=` commit (`check-dts-timebase.py --rtl=`) whenever the
  last banner in the serial log names a commit the DTS does not, so `model` in
  /proc/device-tree names the BITSTREAM, not git HEAD at `make dtbs` time (the W6 gate's
  tree moved two commits during its own build; Tommy's "what is on the board" of 09-06 was
  this). Tested offline; the next board boot is the live test.
  NOTE for the record: the Geekbench run started 04:05 (gb5-w5-raw.log, its result page)
  carries model "SmolRV64 ooo2 f227a2e4 @ 166.67 MHz" -- the main checkout's HEAD when
  board-gate.sh made the DTB, a plan-only commit whose RTL is NOT the full stack -- while
  the bitstream is 9b3050f9 (the banner, gate-results/9b3050f9/board.log). Read that page
  as 9b3050f9.
- 2026-09-07 05:05, gate W6 (build only, ExtraTimingOpt) FAILS TIMING at -0.556 ns, worse
  than W5fix's -0.271 and W5fix3's +0.011 on the same RTL minus the four cuts. Worst path
  u_lq/sqt -> u_iq_i2/e_r, 25 levels, 6.60 ns of which 4.81 ns (73%) is route -- the
  same store-tag source the L1 cut took out of M's completion, reaching the second ALU
  scheduler's ready bits by another road (the LQ's candidate select through the alias
  block into the LSU start and the load's landing wake, present in W5fix's census at 20
  endpoints). Route, not logic: the census's per-family LOGIC delays, W5fix against W6,
  are the honest measure of what the cuts did; running (`make timing`, `make census` on
  the W6 checkpoint).
- 2026-09-07 05:20, the W6 census against W5fix's, per family (rows print twice; deduped):
  the cuts did what they were built for on LOGIC -- fe/u_fetch -> u_fetch 1.84 -> 1.56 ns,
  -> the I$ banks 1.27 -> 1.08, ps_out0 -> m_result 20 -> 17 levels 1.46 -> 1.24, ps_out_a
  -> alu_q_val 1.41 -> 1.20 -- and ROUTE rose 0.3-0.9 ns on every family (u_fetch 3.90 ->
  4.53, m_result 4.18 -> 5.04, alu_q_val 4.28 -> 4.87): 6,117 negative endpoints against
  3,886. The L1 cut moved nothing on the u_lq/sqt families (logic 1.47 -> 1.71): the store
  tags still reach the schedulers and the issue port's payload by the other road, the
  LQ's candidate select through the alias block (`l_block`) into the LSU start and the
  landing wake, 25 levels. So: (1) logic cuts of 0.2-0.3 ns are inside the placement
  scatter build.tcl already documents (81-400 ps between directives on identical RTL) --
  a single gate cannot judge them, and the honest measure is the census's logic column;
  (2) the design is ROUTE-bound at 166.67 MHz, 70-75% of every failing path; (3) W6's cuts
  are kept (correct, cycle-exact, less logic) and get a second roll on the build's default
  directive (AltSpreadLogic_medium, build only, queued behind the overlay harnesses).
  The next structural target if the roll fails is the alias block itself: `l_block` per
  load registered the same way as `l_older` (monotone once the load's address is in,
  computed from the arriving row on the fill cycle), which takes the store tags out of
  the LSU start and the landing wake -- the road L1 left.
- 2026-09-07 05:45, W6's fan-out report (report_high_fanout_nets and the nets recurring on
  the 200 worst paths): `u_lsu/u_mmu/pt_v` (fan-out 65) is on 142 of them, `ld_land` (40)
  on 140, the L scheduler's `rl_iss_v` (456) and the issue payload `pl_q0` (370) on 128;
  `u_lq/wa_ld[*]` -- the landing load's PRF write address, a LUTRAM read fanning to 1,290
  pins (every PRF replica's write port) -- is the one high-fanout net with negative slack
  (-0.14 to -0.18). Why pt_v is on the landing's paths: ooo2_lsu's `pt_done` is one
  `acc_done` for loads and stores, and the store terms' `st_fin` selects on `eff_cbo`,
  which selects on `pt_start` -- this cycle's new start -- so a landing wake began at the
  LQ's candidate select through the alias block. wip/w7 (wt-w7, off wip/w6): (L2) the
  alias block registered per load in ooo2_sq (`l_block_q`; the fill cycle computes it
  from the arriving row `fill_row`, now computed once for the conf write too, against
  av_next), the load queue's candidate select reads the copy, the live block is the oracle
  (a load never starts while the live block holds it, asserted in the core); (L3) the LSU
  exports `pt_ld_done` from the load terms alone (S_LD/S_LD2, the D$'s registered rvalid,
  xword_q, own_pt, own_pt_st), asserted equal to `pt_done & ~pt_is_store`, and ld_land
  reads it. Lint clean, all seven ooo2 benches pass; riscv-tests running; the 60 M cosim,
  the netlist boot and two build-only gate rolls (the default directive, then
  ExtraTimingOpt) are queued behind the overlay harnesses in place of W6's re-roll.
- 2026-09-07 05:55, W7 riscv-tests 240/0; committed on wip/w7 as 6c1eeff1 (the cuts, the
  benches' new pin, the spec). Geekbench on the board: 14 of 21 single-core subtests at
  05:50, 97% CPU, zero faults; the board's clock reads seven hours ahead of the host's.
- 2026-09-07 06:22, THE GLIBC HARNESS RUNS ITS TEST FOR THE FIRST TIME (main 12029862, the
  padded overlay): no "Initramfs unpacking failed", "=== S98glibc ===" at ~1.1 G cycles,
  GLIBC-TEST iteration=1 checksum=414556 and iteration=2 checksum=131459 -- dash and the
  glibc coreutils through ld.so in lockstep, no divergence -- before the 1.4 G budget ran
  out (411,432,080 retires). Each iteration is ~150 M cycles, not 50 M; the default budget
  is 1.8 G now. Those two checksums are the yardstick for the next run. The systemd
  harness runs next.
- 2026-09-07 07:40, THE SYSTEMD HARNESS PASSES on main: the board's /usr/lib/systemd/systemd
  and its 20-library closure loaded and relocated by the board's ld.so under lockstep,
  `systemd --version` printed "systemd 257 (257.4...)", SYSD-TEST PASS at ~1.30 G cycles,
  506,474,993 retires in 1.6 G, no divergence -- the very code that killed init 2.4 s into
  every board boot before the page-crossing fix. With the board's zero-fault Ubuntu boot
  and Geekbench past its single-core set, the fix is closed from both sides.
- 2026-09-07 07:50, W7's 60 M cosim: 14,301,801 retires, identical to W5fix3 and W6 -- L2
  and L3 are cycle-exact on the boot too. Gate rolls running (build only): the default
  directive first, then ExtraTimingOpt if it misses.
- 2026-09-07 08:13, GATE W7 MEETS TIMING AT +0.019 ns ON THE DEFAULT DIRECTIVE
  (AltSpreadLogic_medium; build only, gate-results/6c1eeff1/, bitstream banked there) --
  the first full-stack build to close without ExtraTimingOpt. Worst path m_addr ->
  u_iq_i/e_r. One roll, so no claim beyond "the cuts did not hurt and the roll passed";
  the census comes with the board turn. The board finishes Geekbench on W5fix3 first
  (multi-core set, 35 of 42 subtests at 08:15), then tools/board-gate.sh on the banked
  bitstream decides W7. Synthesis, checked: 3:45 on wt-w7's sources, 82,668 CLB LUTs,
  the same size as every full-stack build -- and every gate's synthesis is ~4 minutes at
  4.6 GB, the 31 GB peak in build.tcl's comment is not this flow's.
  Implementation peaks at 6.2-7.6 GB (W7, W5fix3). So a cosim or a test run may share the
  box with a build, and build.tcl's thread cap (chosen for a 31 GB peak) is worth
  re-measuring at 8 threads for a faster gate -- later, on a build nobody is waiting for.
- 2026-09-07 08:20, W7's CENSUS (deduped): 813 endpoints within +0.35 ns and 117 within
  +0.10, against 6,194 / 6,194 on W6, ~3,900 on W5fix and 4,305 on T1F2 (the last main
  build with a census). The u_lq/sqt families -- W5fix's and W6's largest -- are GONE from
  the list. The head now: m_addr -> u_iq_i/e_r (+0.019, 20 paths, 22 levels), the fetch
  buffer's fb_va -> (+0.075, 56 paths, 14 levels), u_rename/h_ld (+0.078, 21 paths),
  ps_out0 -> (+0.103, 37 paths, W). This is the first full-stack netlist with a MARGIN
  distribution rather than a coin flip: five cuts (F1-F3, L1-L3) took the store tags and
  the iMMU's verdict out of the enables, and the placer's basin followed. The board turn
  (after Geekbench) decides; then wip/w7 lands.
- 2026-09-07 09:38, GEEKBENCH 5 ON THE FULL TWO-WIDE STACK (board build 9b3050f9, the
  result page's model string says f227a2e4 -- see 04:40): 5 h 35 min wall (11:02-16:37
  board time) against 9 h 05 min for the 2026-08-28 run on efe6dd6; `perf stat` over the
  whole run 1.298 T instructions in 3.230 T cycles, IPC 0.40, against 1.325 T in 5.133 T,
  IPC 0.26 -- 37% fewer cycles for the same work, IPC +54%. Zero faults through the run.
  Result: https://browser.geekbench.com/v5/cpu/24610038 (the previous ones: 24579000 on
  efe6dd6, 24591512 on 2026-08-31). Counters in gb5-w5-perf.txt. The board is programmed
  with W7 next (board-gate on its banked bitstream).
- 2026-09-07 09:50, W7 BOOTS UBUNTU ON THE BOARD, login 1, faults 0 (banner rtl=6c1eeff1),
  and LANDS: main is 256899f6 (wip/w7's three commits rebased; RTL identical to the gated
  tree), bitstream banked as /var/tmp/ooo2_W7_fullstack_default_166MHz_6c1eeff1.bit. The
  board runs it now. The DTB stamp read the PREVIOUS banner this time (the console log
  flushes late and ubuntu-boot.sh looked before the new banner landed): board-gate.sh now
  waits for the banner past the programming offset and hands it over (RTL_BANNER), so the
  board's model string will say 6c1eeff1 from the next boot on; this one says 9b3050f9.
- 2026-09-07 10:00, STATUS. Items 1-10 of this plan are on main and on the board: two-wide
  fetch (10a), dispatch (10b), retire (10c), the ALU's own port and a second ALU on a
  fourth PRF shard (10d), the run-ahead fetch buffer (10e, with its page-crossing fix), and
  the T1 cuts F1-F3 and L1-L3 that turned the 166.67 MHz coin flip into +0.019 ns on the
  default directive with 813 near-critical endpoints. Measured on the board: sha256sum IPC
  1.32-1.39 (0.90 before the stack), Geekbench 5 in 5 h 35 min at IPC 0.40 (9 h 05 min at
  0.26 on the 2026-08-28 core), result 24610038, zero faults in either. Provenance: tags
  board/W2F-iw2disp (319cd248), board/T1F3-threecuts (ca457d95), board/W5FIX3-fullstack
  (9b3050f9), board/W7-fullstack-sixcuts (6c1eeff1); bitstreams in /var/tmp; evidence in
  gate-results/<sha>. Still open, in order of value: the L scheduler's pick fan-out
  (`rl_iss_v`, 456 loads into the 370-bit payload mux) and `wa_ld` (1,290 loads) if a
  census ever leads with them; ROB-32 (wip/v6, wt-rob32) as the next IPC item; the 4-state
  ROB completion assertion under xsim; the glibc harness's four iterations at the 1.8 G
  budget as a periodic check; build.tcl's thread cap re-measured for a faster gate.
- 2026-09-07 10:15, Tommy reads the result page: GB5 score 5, the new high (the 2026-06-26
  baseline in workloads/gb5/baseline-scaled.md is 0.9 single-core after its ÷10). The CPI
  STACK of the whole run (tools/perf-cpi-stack.py --width 2 on gb5-w5-perf.txt; the 13-event
  `cpi` set, so FE_ALN/FE_QUE are not split out), against the 2026-08-28 run on efe6dd6
  (width 1):
                                 2026-08-28 efe6dd6     2026-09-07 full stack
    CPI                          3.873 (IPC 0.258)      2.488 (IPC 0.402)
    dispatch floor               1.000                  0.500 (two-wide)
    LSU (D$/dTLB/AMO)            2.146  55%             1.182  47.5%
    FPU                          1.081  28%             0.484  19.5%
    serializing op               0.151                  0.164
    multiplier / divider         0.036 / 0.014          0.036 / 0.010
    frontend bubble              0.216                  0.099 (iMMU 0.010, no bytes 0.034)
    unattributed (width-2)       --                     0.014
    redirects MPKI               10.8                   6.4
    D$ MPKI / miss rate          8.6 / 1.98%            8.3 / 2.30%
    I$ MPKI                      0.44                   0.48
    LSU stall per D$ access      4.93 cycles            3.28 cycles
  Read: the two-wide stack halved the dispatch floor and took 0.96 CPI out of the LSU
  stall (the senior store queue, the early load start, the run-ahead fetch keeping the
  window full), 0.6 out of the FPU stall (a dependent FP wait that the deeper window hides)
  and 0.12 out of the frontend; redirects fell 40% (fewer mispredict restarts per
  instruction, the predictor trained by the two-wide bundles). What is LEFT is 47% LSU --
  3.3 cycles of stall per D$ access at a 2.3% miss rate, i.e. HIT latency and the
  load-to-use chain, not misses -- and 20% FPU. The next IPC item that moves Geekbench is
  therefore the load path (the D$ hit latency / load-to-use, and ROB-32 so more loads
  overlap), not the frontend, which is down to 4%.
- 2026-09-07 10:25, the single-core table (workloads/gb5/result-2026-09-07.md, from Tommy):
  every subtest 4-10x the June baseline by score EXCEPT Machine Learning -- 0 at 0.01
  images/sec, the same rate as June. 100 s per MobileNet image at 166 MHz is ~16 G cycles
  for ~0.6 G multiply-adds: ~30 cycles per MAC, which is a trap per few operations
  (misaligned accesses emulated by OpenSBI, an FP instruction the core lacks) or an FPU
  slow path, not a normal FMA chain. The preview binary has no per-workload switch, so the
  instrument is a full run with the counters sampled every 10 s system-wide (`perf stat -a
  -I 10000`: cycles, instructions, redirects, TRAP redirects r0008, LSU/FPU/serializing
  stalls, the frontend bubble, D$/I$ accesses and misses) beside Geekbench's own "Running
  X" lines stamped with the same clock -- /var/tmp/gb5-trace.sh, launched 10:25 on the W7
  board (5.5 h). Each subtest's window then gets its own stack; Machine Learning's decides.
  Also worth a look when it lands: Camera (2), Structure from Motion (2) and Gaussian Blur
  (1) sit below the rest.
- 2026-09-07 11:15, the trace's first eleven subtests (tools/gb5-trace-stack.py): IPC 0.74
  AES-XTS, 0.43 Text Compression, 0.53 Image Compression, 0.31 Navigation, 0.33 HTML5,
  0.30 SQLite, 0.36 PDF, 0.39 Text Rendering, 0.28 Clang, 0.42 Camera, 0.21 N-Body. LSU
  40-57% everywhere; the frontend bubble 23-26% on SQLite and Clang (I$ miss 1.2-2.0%,
  40-44 redirects/K: the branchy, code-heavy ones want a bigger I$ and a better predictor);
  N-Body 26% FPU and a 5.9% D$ miss rate (32 MPKI: its footprint misses the D$; prefetch is
  the item that was switched off for the board's DMA). And "SER" reads 6-13% in EVERY
  subtest -- which is not serializing ops: `st_ser` in ooo2_core is the residual "M could
  advance, dispatch did not, and it was not a load or FP dependency or the ROB" -- so it
  holds the INTEGER dependency waits (SH_IE/SH_IE2 results the schedulers block on), the
  store/load queue and scheduler fulls, the rename free lists, and only then the true
  serializing drains. That bucket is 0.15-0.4 CPI per subtest and its name hides the
  next IPC question (integer latency chains vs queue capacity). To do, counters only:
  split it into dep_ie, sq/lq full, iq full, ser proper (gen-perf-events.py regenerates
  the event table from csr_file.v; the stack tool's identity check keeps it honest).
- 2026-09-07 11:15, done on main (3bb7947f): ST_DSP keeps r0304 as the whole bucket (so
  the `cpi` set stays a complete stack and every saved perf file still adds up), and
  ST_IQ/ST_RN/ST_SQ/ST_LQ/ST_SRZ (r0306-r030a) are its disjoint parts in d_hold's order;
  `perf-smol.sh hold` collects them and perf-cpi-stack.py prints them under the bucket.
  Lint clean, the boot cosim exact; build-only gate running, the board turn after the
  trace run. The first `hold` run on Geekbench (a 5.5 h run per set) says whether the
  0.16-0.4 CPI is scheduler capacity (integer chains), queue capacity or rename.
- 2026-09-07 11:55, the split's gate: timing met at +0.056 ns (default directive, build
  only, gate-results/3bb7947f/); the board turn follows the trace run. Meanwhile the
  trace's Gaussian Blur window (43 min, score 1): IPC 0.40, LSU 41%, FPU 31%, 0.04
  traps/K -- a normal stack. Its score is the instruction count: scalar RISC-V without the
  vector extension does that work in many times the reference machine's SIMD instruction
  count, which is an ISA gap, not a stall. Camera and Machine Learning likely read the
  same way unless ML shows a trap storm; its window is next.
- 2026-09-07 12:55, MACHINE LEARNING'S OWN STACK (single-core window, 466 s): 28.3 G
  instructions in 78.5 G cycles, IPC 0.36; traps 0.04 per thousand (1.2 M in all --
  no trap storm, no emulation), redirects 2.6/K, LSU 61%, FPU 21%, dispatch hold 1%,
  frontend 1%; D$ 11.5 G accesses at a 4.4% miss rate (18 MPKI), I$ 0.06%. So the core
  is NOT doing anything pathological on it: it runs at a normal IPC, and the 0.01
  images/sec is the instruction count -- about 6 G instructions per image for a MobileNet
  of ~0.6 G multiply-adds, ten scalar instructions per MAC (load, widen, multiply-add,
  address and loop work), where the reference machine's SIMD does the same in a small
  fraction. Gaussian Blur (IPC 0.40), Structure from Motion (0.43) and Speech (0.27)
  read the same way. That is the ISA (no vector extension), not a stall, and no IPC item
  reaches it; the one lever the core has on ML is its 61% LSU share at a 4.4% D$ miss rate
  -- the weights streaming through the D$ -- i.e. prefetch (item 6, off since the board's
  DMA corruption) or a larger D$. Speech Recognition and N-Body are the same shape (LSU
  57%, D$ miss 4-6%, 16-32 MPKI). The trace continues through the multi-core set; the
  split's board turn follows.
- 2026-09-07 13:30, TAKING STOCK (Tommy: "a milestone -- the first meaningful improvement
  and solidly superscalar"). What the plan believed on 2026-09-05 and what the week says:
  (1) "sha256sum is frontend-bound (41.8% FE) and Geekbench is not; do not generalise a
  CPI stack from one workload" -- held exactly: the frontend items (10a, 10e, the fetch
  cuts) took sha256sum from 0.71 to 1.35 IPC, and Geekbench, per subtest now, is LSU 40-
  60% everywhere with the frontend at 1-8% (23-26% only on SQLite and Clang, from I$
  misses and 40+ redirects/K). (2) The items in measured order were the right order:
  10b/10c/10d (two-wide dispatch/retire, two ALUs) halved the dispatch floor and the
  deeper window took 0.96 CPI off the LSU stall and 0.6 off the FPU wait on Geekbench --
  the stack's biggest single move -- without any LSU change. (3) NEW: closure at 166.67
  MHz is route-bound and a single build is a ~0.3 ns coin flip; what moved the placement
  basin was taking LUTRAM-sourced predicates out of high-fan-out ENABLES (the store tags
  out of pt_v/ld_land, the iMMU verdict out of imem_ok), not shaving data paths (W6's
  logic-only cuts made the basin worse). 813 near-critical endpoints against ~4,000-6,000
  is the first margin distribution this design has had. (4) NEW: three Geekbench scores
  (Gaussian Blur, Structure from Motion, Machine Learning) are the missing vector
  extension -- normal IPC, 6 G scalar instructions per ML image -- and no IPC item
  reaches them; Tommy's read is right that a better instruction mix needs GB7 with V, or
  SPEC built for rv64gc. (5) NEW: three harness lessons that each cost a day -- a Vivado
  function fold (rule F4/F5), an overlay cpio at an unaligned offset (no harness had ever
  run its test), the "serializing" counter that was every dispatch hold. (6) NEW: the
  board's userspace deaths reproduce in lockstep given enough cycles (tiny128's busybox
  is dynamic; 1.4 G cycles reaches ld.so), so "a cosim that runs the failing code" is a
  method, not a hope. Pending now: the traced run's multi-core set (~1.5 h), then the
  counter split's board turn; nothing else is queued.
- 2026-09-05, gate V5 (b86bbc3c: V4 + NF 8 -> 5 + the monitor-boot LSU alignment): FAIL,
  WNS -0.968 ns -- a smaller design placed a nanosecond worse than V4's 0.000, which says
  the two-wide core sits about a nanosecond inside the placer's variance on three
  route-dominated families (75-80% route, ~5 ns of it): (F) the fetch cycle, from the
  interrupt-inject or the PC through the iTLB compare, the fetch buffer's window, the
  aligner and into the F/X queue's data pins (17-20 levels); (D) M's completion into
  dispatch: `fr_set` (a mispredict resolved in M that cannot redirect yet, which is
  `~redirect`, which is ld_land and the store queue's drain) reached `d_take` through
  `fr_active` and from there rename, the free lists and the pending table (22-25 levels)
  -- the gate V3's fix had freed d_take of `redirect` and this term let the same cone
  back in; (W) the M port's PRF read (address fan-out 387) into address/compare. V6 takes
  (D) and the V4 chain: dispatch holds on the registered `fr_v` (one cycle of wrong-path
  dispatch per deferred mispredict, the flush's to take), and fetch's window validity is
  gated on a registered context change (every context change arrives with a redirect and
  the buffer empties at the same edge). (F) and (W) are structural: a second frontend
  stage, and fewer PRF read replicas; they are the next builds if V6 lands short.
- 2026-09-05, gate V6 (21ac5b69: V5 + dispatch on the registered fr_v + fetch's window on
  a registered context change): FAIL, WNS -0.319 ns on 4,172 endpoints (TNS -601; V5:
  -0.968 on 10,679, TNS -5381). What is left is structural: (L) the landing load's yield
  reaching the M/F issue register's enable through the load queue's ordering decision,
  (F) the fetch cycle round `pc_q`, and the LSU's write into the D$ door at 81% route.
  V7 adds the interrupt inject's registered enable; (F) and (L) are the next two builds.
||||||| parent of b86bbc3c (ooo2: NF 8 -> 5 -- the two-wide core closed at exactly 0.000 ns and did not boot; FP gives first)
